# ── 올리면 안 되는 것 ────────────────────────────────────────────
# 로그인 정보·쿠키·브라우저 사용 기록은 **절대** 올라가면 안 된다.
# (BARODU Tools 의 크롬 칸은 이 컴퓨터 안에만 있고 여기에 들어오지 않지만,
#  실수로 복사해 두는 일이 없도록 이름을 미리 막아 둔다)
*chrome-profile*
*smartstore-profile*
*browser-profile*
*.cookies
cookies*
session*
*secret*
*token*
*.pem
*.key
.env
.env.*

# ── 프로그램이 알아서 만드는 것 ──────────────────────────────────
node_modules/
dist/
*.tsbuildinfo
.vite/

# ── 컴퓨터가 남기는 것 ──────────────────────────────────────────
Thumbs.db
ehthumbs.db
desktop.ini
.DS_Store

# ── 잠깐 쓰고 버리는 것 ─────────────────────────────────────────
*.log
tmp/
temp/
.cache/

# ── 개인 설정 ──────────────────────────────────────────────────
.claude/
.vscode/
.idea/
.wrangler/
