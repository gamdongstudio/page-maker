# SAY PAGE MAKER PRO — 작업기록

> 이어서 작업할 때 전체 코드를 다시 읽지 않아도 되도록 여기부터 보세요.

---

## 실행 방법

```bash
npm install     # 처음 한 번만
npm run dev     # http://localhost:5180
npm run build   # 배포용 빌드
```

기존 프로젝트(HOMIO, PAGE-MAKER, PAGE-MAKER-SMARTSTORE 등)는 건드리지 않았습니다.
모든 작업은 `SAY-PAGE-MAKER-PRO` 폴더 안에서만 했습니다.

---

## 현재 상태 (2026-09-14) — PHASE 1~9 + MASTER 잔여 항목 완료

| PHASE | 내용 | 상태 |
|---|---|---|
| 1 | 프로젝트 구조 · 좌우 레이아웃 · 미리보기 · 편집패널 · 데이터 모델 · 자동저장 | 완료 |
| 2 | 상품정보 · 사진 업로드 · 카드형 사진관리 · 자동맞춤 · 순서변경 | 완료 |
| 3 | 메뉴 추가/편집/복제/숨기기/삭제/드래그 · 미리보기 연결 | 완료 |
| 4 | 디자인 프리셋 · 색상 · 폰트 · 여백 · PC/모바일 · 좌우 폭 조절 · 패널 숨기기 | 완료 |
| 5 | 프로젝트 저장/불러오기 · 실행취소/다시하기 · 새로고침 복원 | 완료 |
| 6 | 전체 이미지 · 분할 JPG · ZIP · 품질 확인 | 완료 |
| 7 | AI 서비스 구조 · 제목/메뉴/카피 추천 · 전체/선택 적용 | 완료 (AI 미연결, 기본 추천으로 동작) |
| 8 | SAY PROMPTER 연결 구조 · GPT/Claude/Codex 출력 UI | 완료 (요청문 생성까지) |
| 9 | 스마트스토어 체크 · LITE/PRO · 회귀 테스트 · 모바일 테스트 | 완료 |

### 2차 작업 — MASTER 요구사항 중 빠져 있던 것 (완료)

| 항목 | MASTER 위치 | 상태 |
|---|---|---|
| 참고 이미지/캡처 분석 | PHASE 8 · [참고 이미지/캡처 분석] | 완료 (실제 동작) |
| 대표/대문 이미지 모양 선택 | [대표/대문 이미지] | 완료 (자동추천/가로형/정사각형/세로형) |
| 제작 방식 3가지 | [제작 방식 3가지] | 완료 (첫 화면에서 선택) |
| LITE / PRO 노출 범위 | [LITE / PRO] | 완료 (기능별로 나눔) |

### 3차 작업 (완료)

| 항목 | 내용 | 상태 |
|---|---|---|
| 간편 스타일 '강렬한' 제거 | 사용자 요청 — 7종 → 6종 | 완료 |
| 사진이 많을 때 자동저장 실패 | 저장소를 넉넉한 곳으로 옮김 | 완료 (18MB 사진 12장 저장·복원 확인) |

### 4차 작업 (완료)

| 항목 | 내용 | 상태 |
|---|---|---|
| 사진 업로드 자동 최적화 | 올릴 때 상세페이지에 알맞은 크기로 줄임 | 완료 (9.04MB → 2.05MB) |

### 5차 작업 — 사진관 전용 도구로 방향 전환 (1단계 완료)

범용 쇼핑몰 상세페이지 제작기 → **사진관 전용 AI 자동 상세페이지 도구**.

| 항목 | 상태 |
|---|---|
| 첫 화면을 자동 제작 중심으로 (STEP 1·2·3 + 큰 버튼) | 완료 |
| 촬영상품 카탈로그 (추가·수정·순서·복제·숨기기·삭제·기본 되살리기) | 완료 |
| 사진관 공통정보 (한 번 적고 모든 상품에서 재사용) | 완료 |
| 촬영상품별 전체 자동 생성 (구성·순서·제목·글·사진배치·대표사진) | 완료 |
| "이렇게 읽었습니다" 확인 화면 (틀리면 안 되는 값은 확정하지 않음) | 완료 |
| 사진관 전용 섹션 4종 (특별한 혜택·촬영 콘셉트·촬영 과정·준비사항) | 완료 |
| 장점 / 이런 분께 추천 생김새 분리 | 완료 |
| 용어 변경 (촬영상품 정보 · 사진·영상 · 상세페이지 구성 · AI 자동 작성 · 완성·저장) | 완료 |
| 원본 인물 보호 규칙 문서화 + 화면 안내 | 완료 |
| 섹션 템플릿 자리(`template`) · 부분 재생성 함수 | 자리만 만듦 (2단계에서 연결) |

### 5차 작업 2단계 — 섹션 템플릿 + 부분 수정 (완료)

| 항목 | 상태 |
|---|---|
| 섹션 모양(템플릿) 고르기 — 이벤트4·혜택5·가격4·콘셉트5·장점4·추천4·갤러리4·과정2·준비2 | 완료 |
| 섹션마다 [글 수정] [사진 변경] [디자인 변경] [AI로 다시 만들기] | 완료 |
| 부분 재생성 — 전체 / 제목만 / 문구만 / 사진 배치만 / 디자인만 | 완료 |
| 모양을 바꿔도 글·사진·가격 유지 | 완료 (확인함) |
| 한 섹션만 고쳐도 다른 섹션 안 바뀜 | 완료 (확인함) |
| 전체 다시 만들기 3선택 (디자인만 / 구성·글 새로 / 완전히 새로) | 완료 |

### 5차 작업 3단계 — 자동 생성 '정확도' 올리기 (완료)

| 항목 | 상태 |
|---|---|
| 사진관 자료에서 정보 읽기 (16항목) | 완료 — 붙여넣은 글에서 |
| 읽은 값 확인·수정 화면 + [확인하고 적용] | 완료 |
| 못 찾은 값은 지어내지 않고 [확인 필요] 표시 | 완료 |
| 촬영사진 역할 자동 추천 (비율·밝기·색·비슷한 사진 묶기) | 완료 |
| 실제 정보로 제목·카피·추천대상 만들기 | 완료 |
| 내용 양·사진 모양을 보고 템플릿 자동 선택 | 완료 |
| 만든 뒤 [이대로 사용] [부분 수정] [전체 다시 만들기] | 완료 |
| 샘플 내용이 결과에 섞이지 않게 | 완료 — 자료를 넣는 순간 샘플을 비우고, 내용 없는 섹션은 숨김 |
| 캡처 사진 속 글자 읽기 (OCR) | **하지 않음** — 아래 이유 |

### 6차 작업 — 한 화면으로 합치기 (완료)

AI 제작기와 수동 편집기를 나누지 않는다. AI 는 ①~④ 항목을 **자동으로 채워주는 기능**이다.

| 항목 | 상태 |
|---|---|
| AI 전용 시작 화면 제거 → ⑤ 패널로 통합 | 완료 |
| 상단 [AI로 만들기] → ⑤ 패널 열고 이동·포커스 | 완료 |
| ⑤ 안에 STEP 1~6 (사진·촬영상품·붙여넣기·요청·범위·만들기) | 완료 |
| AI 생성 범위 6가지 (기본 '비어 있는 내용만 채우기') | 완료 |
| 생성 진행 표시 7단계 · 만든 것부터 미리보기 반영 | 완료 |
| **촬영상품이 ① 로 전달되지 않던 오류** | 완료 (원인 아래) |
| 커피 샘플 제거 → 촬영상품별 사진관 초안 | 완료 |
| 사진관 용어 (사진관명·촬영 종류·추천 대상·예약·문의하기 …) | 완료 |
| 사진관 기본 구성 확장 (최대 16섹션, 내용 없으면 숨김) | 완료 |
| 편집 단계 상태 표시 + [다음: …] 이동 버튼 | 완료 |
| 디자인 스타일 카드 (실제 색·글꼴·버튼·모서리 미리보기) | 완료 |
| 상단 메뉴 정리 (항상 4개 + 더보기) | 완료 |
| LITE/PRO → 간편 편집 / 상세 편집 + 전환 전 안내 | 완료 |
| SAY PROMPTER 설명 + '사용할 AI 선택' 단순화 | 완료 |
| 사진 사용 위치 용어 (메인·상품소개·촬영 구성·보정 전후·가격·혜택·사진관 소개) | 완료 |

### 9차 작업 — 링크로 가져오기 (완료)

기존 `naver-blog-auto` (공부 2 폴더) 의 Playwright 수집 구조를 재사용했다.

| 항목 | 상태 |
|---|---|
| 수집 서비스 `collector/` (Express + Playwright) | 완료 |
| 네이버 블로그 (iframe #mainFrame 처리) | 구현함 — 실제 블로그로는 미검증 |
| 스마트플레이스 (entryIframe 처리) | 구현함 — 실제 주소로는 미검증 |
| 일반 홈페이지 | 완료 (실제로 확인) |
| 가져온 정보 확인 화면 (항목별 체크) | 완료 |
| 기존 사용자 값 우선 (충돌 시 꺼둠 + 경고) | 완료 |
| 사진 골라서 가져오기 (로고·아이콘 1차 제외) | 완료 |
| 오류 시 기존 작업 보존 | 완료 |
| 수집 서비스 꺼져 있으면 켜는 법 안내 | 완료 |

**재사용한 것**: `server/lib/browser.js` 의 `openScrapeContext` (로그인과 분리된 일회용 컨텍스트,
UA·로케일 설정, navigator.webdriver 제거).
**가져오지 않은 것**: 로그인 세션, 글 작성·발행, 이미지 생성. 기존 프로그램은 건드리지 않았다.

**읽어온 글은 기존 `parseStudioText` 에 그대로 넣는다.** 붙여넣기용 읽기 기능을 두 벌 만들지 않았다.

### 9차 작업 테스트 (localhost 실제 실행)

| 항목 | 결과 |
|---|---|
| URL 입력 영역 표시 (링크/붙여넣기 전환, 기본 링크) | 통과 |
| 일반 홈페이지 (example.com) | 통과 — 제목·본문 읽음 |
| 사진관 형태 페이지 (직접 만든 시험 페이지) | 통과 — **16항목 전부** 추출 |
| 상호에서 페이지 제목 꼬리 제거 | 통과 — '감동사진관 | 광명 가족사진' → '감동사진관' |
| 잘못된 URL | 통과 — 안내 + "현재 작업 내용은 그대로 있습니다" |
| 바로 덮어쓰지 않음 | 통과 — 확인 화면 먼저 |
| 기존 값 보호 | 통과 — 상호·이벤트가 충돌 항목이 꺼진 채 경고 표시, 적용해도 기존 값 유지 |
| 사진 썸네일 · 로고 제외 | 통과 — 60×60 로고 제외, 큰 사진 2장만 |
| 사진 골라 가져오기 → 기존 사진 관리 연결 | 통과 (받아오지 못한 사진은 그렇게 알림) |
| 상품정보 탭에도 같은 값 | 통과 |
| 자동저장 · 새로고침 복원 | 통과 |
| 네이버 블로그 / 스마트플레이스 실제 주소 | **확인 불가** — 시험할 실제 주소가 없었다 |
| 빌드 | 통과 |

**페이지 구조가 바뀌면 고칠 곳**: `collector/collect.js` 의 `EXTRACTORS` 와 `readPage`.

### 8차 작업 — 아이콘 메뉴 · 내 작업 · 완성 화면 (완료)

| 항목 | 상태 |
|---|---|
| 첫 화면 '어떻게 시작할까요?' (AI / 직접 입력 / 이어서 하기) | 완료 |
| HOMIO 방식 아이콘 메뉴 8개 (아이콘 + 글자) | 완료 |
| 한 번에 **한 도구만** 펼치기 (세로 스크롤 줄임) | 완료 |
| 내 작업 — 이름 저장 · 열기 · 복제 · 이름변경 · 삭제(확인창) | 완료 |
| 최근 보관 작업을 첫 화면에 표시 | 완료 |
| 완성 화면 '처음이라면 추천' + 다른 저장 방법 접기 | 완료 |
| 완성도 체크 (기존 데이터만 재사용) | 완료 |
| AI 패널 닫기 버튼 | 완료 |
| AI 생성을 **실행취소 한 번**으로 되돌리기 | 완료 |
| 예약·문의 버튼 최대 3개 (예약/전화/카톡) | 완료 |
| SAY PROMPTER 에 담을 내용 고르기 (상품·디자인·구성·사진배치) | 완료 |
| 인라인 글자 도구 · 섹션별 개별 글꼴 | **하지 않음** (아래 남은 문제) |

### 8차 작업 테스트 (localhost 실제 실행)

| 항목 | 결과 |
|---|---|
| 첫 진입 — AI 전용 페이지 없음, 좌우 함께 | 통과 |
| 아이콘 8개 · 누를 때마다 패널 1개만 | 통과 (8개 전부 확인) |
| 가족사진 선택 → 상품정보 연결 | 통과 |
| 상품명 수정 → 미리보기 즉시 반영 | 통과 |
| 내 작업 저장 → 복제 → 열기 → 계속 수정 | 통과 |
| AI 생성 (사진 5장 + 가격 붙여넣기) | 통과 — 11섹션 |
| **실행취소 한 번으로 AI 적용 전 복귀** | 통과 (11 → 3섹션) |
| 완성도 체크 | 통과 — 33% → 100% 로 바뀌는 것 확인 |
| 처음이라면 추천(분할 JPG) | 통과 — 4장 |
| 전체 이미지 | 통과 — 1720×8600 |
| 새로고침 복원 | 통과 |
| 모바일 375px | 통과 — 미리보기 위, 가로 스크롤 없음, 아이콘 메뉴 안 잘림 |
| 빌드 · 콘솔 오류 | 없음 |

작업 중 고친 것:
- 카드형 단계를 걷어내면서 `StepCard` 를 지웠는데 `StepProduct` 가 그 안의 입력 부품을 쓰고 있었다.
  → `components/editor/Fields.tsx` 로 입력 부품만 따로 빼고 연결했다.
- Vite 개발 서버가 파일 교체를 따라오지 못해 빈 화면이 된 적이 있다. 서버를 다시 켜서 해결.
  (빌드는 정상이었다 — 개발 서버만의 문제)

### 7차 작업 — 글꼴 · SAY PROMPTER · 정직한 진행 표시 (완료)

| 항목 | 상태 |
|---|---|
| 글꼴 5종 (Pretendard·Noto Sans KR·Spoqa Han Sans Neo·Gmarket Sans·나눔고딕) | 완료 |
| 같은 예문으로 글꼴 미리보기 비교 | 완료 |
| 고른 글꼴만 불러오기 · 굵기 2가지만 · 실패 시 대체 글꼴 | 완료 |
| **저장 이미지에도 같은 글꼴 적용** | 완료 (확인함) |
| 간편 스타일을 바꿔도 직접 고른 글꼴 유지 (+되돌리기) | 완료 |
| 옛 작업파일의 글꼴 이름 자동 변환 | 완료 |
| SAY PROMPTER 기본형/ChatGPT용/Claude용 (Codex 제거) | 완료 |
| 유형 바꿔도 적은 글 유지 | 완료 |
| AI 실패 시 기존 작업 보존 + 안내 | 완료 |
| 전체 생성을 실행취소 **한 번**으로 되돌리기 | 완료 |
| 진행 표시를 실제로 하는 일 3단계로 (가짜 단계 제거) | 완료 |
| 사진 '사용하지 않음' 위치 추가 | 완료 |
| 참고 캡처 — 글자는 못 읽는다고 명시 | 완료 |

---

## 폴더 구조

```
src/
  config/smartstore.ts        규격값 한 곳에서 관리 (폭 860px, 용량, 검수 기준)
  types/project.ts            데이터 모델 (저장 형식)
  types/defaults.ts           메뉴 후보 22종 · 디자인 프리셋 7종 · 샘플 상품
  store/ProjectStore.tsx      상태 + 자동저장 + 실행취소/다시하기
  services/
    storage/db.ts             브라우저 안쪽 저장소 (사진이 많아도 담긴다)
    storage/local.ts          자동저장 · 작업파일 저장/불러오기
    ai/types.ts               AI provider 인터페이스 (금지 표현 필터 포함)
    ai/localPlanner.ts        AI 연결 전 기본 추천기 (규칙 기반)
    ai/index.ts               provider 등록소
    export/exportImage.ts     이미지 생성 · 자연스러운 분할 · ZIP
    export/checkStore.ts      스마트스토어 체크
    prompter/buildPrompt.ts   SAY PROMPTER 요청문 생성
    reference/analyzeCapture.ts  참고 캡처에서 색·여백·구성 읽기
  store/EditionContext.tsx    LITE / PRO 노출 범위
  components/
    start/StartChoice.tsx     제작 방식 3가지 고르기 (처음 한 번)
    design/ReferenceCapture.tsx  참고 캡처에서 느낌 가져오기 (PRO)
    layout/SplitLayout.tsx    좌우 분할 + 드래그 경계선 + 모바일 세로 배치
    preview/DetailPage.tsx    상세페이지 본체 (미리보기와 출력이 같은 컴포넌트)
    preview/Preview.tsx       미리보기 틀 (PC/모바일/전체화면)
    editor/EditorPanel.tsx    오른쪽 7단계
    editor/StepProduct.tsx    ① 상품정보
    media/StepMedia.tsx       ② 사진·미디어
    menus/StepMenus.tsx       ③ 메뉴 구성
    design/StepDesign.tsx     ④ 디자인
    ai/StepAi.tsx             ⑤ AI 만들기
    ai/StepPrompter.tsx       ⑥ SAY PROMPTER
    export/StepCheck.tsx      ⑦ 검수·완성
    export/ExportStage.tsx    출력용 실제 크기(860px) 숨은 렌더링
  utils/image.ts              사진 자동맞춤 (핵심)
  utils/format.ts             값 표시 도구
```

---

## 중요한 설계 결정

**1. 미리보기 = 출력물**
`DetailPage.tsx` 하나를 미리보기와 이미지 출력이 같이 쓴다.
"미리보기만 예쁘고 실제 결과가 다른" 문제를 구조적으로 막는다.
화면에서 줄여 봐도(`transform: scale`) 출력은 항상 860px 원본 크기로 뽑는다 (`ExportStage`).

**2. 사진 자동 맞춤** (`utils/image.ts`)
- 원본 비율 유지, 찌그러짐 없음
- 원본 비율과 거의 같으면 아예 자르지 않는다 (오차 6% 이내)
- 세로로 아주 긴 사진은 폭의 1.45배까지만, 납작한 가로 사진은 0.42배 이상
- 인물 얼굴 보호를 위해 기본 초점을 조금 위(`focusY: 42`)로 둔다
- 화면에는 `object-fit` 같은 말을 쓰지 않고 "자동 맞춤 / 사진 전체 보기 / 화면 꽉 채우기"로 표시

**3. 규격값은 한 곳에서**
`config/smartstore.ts` 의 `SMARTSTORE_DETAIL_WIDTH`, `IMAGE_POLICY`, `CHECK_RULES`.
정책이 바뀌면 이 파일만 고치면 된다.

**4. AI 는 없어도 동작한다**
`AiProvider` 인터페이스만 구현하면 실제 AI 를 붙일 수 있다.
지금은 `localPlanner`(규칙 기반)가 동작하며, 화면에
"입력한 상품정보를 규칙대로 정리한 결과입니다. (AI 연결 전)" 라고 **그대로 밝힌다.**
가짜로 AI인 척하지 않는다. 고급 AI 자리는 `isReady() === false` 로 연결 안내만 보여준다.

**5. 금지 표현 차단**
`stripBanned()` 이 "상단노출 보장 / 검색 1위" 등을 결과에서 제거한다.
검색용 상품 제목과 상세페이지 대문 카피를 분리해서 추천한다.

---

**6. 참고 캡처는 '읽은 것' 만 말한다**
캡처에서 실제로 잰 값만 보여준다. 배경색·대표색·글자색·여백·구간 수·사진 비율·아래쪽 버튼 유무.
못 잰 것은 "짐작하기 어려웠어요" 라고 그대로 말한다.
본문 글씨 크기는 줄여서 볼 때 여러 줄이 뭉쳐 믿을 수 없어 **아예 짐작하지 않는다.**
AI 가 아니라 색과 여백을 직접 잰 결과라고 화면에 밝힌다.
캡처 사진 자체는 저장하지 않고(용량), 읽어낸 결과만 작업파일에 남긴다.
남의 페이지를 픽셀 단위로 베끼지 않는다는 점도 화면에 적었다.

**7. LITE / PRO 는 프로그램을 나누지 않는다**
7단계 구조는 그대로 두고 각 단계 **안에서** 보여주는 범위만 줄인다.
(단계가 통째로 사라지면 초보자가 더 헷갈린다)

| | LITE | PRO |
|---|---|---|
| 메뉴 종류 | 기본 10종 | 전체 22종 |
| 디자인 | 프리셋 + 색상 4가지 | + 크기·여백·글씨·버튼·정렬 |
| 참고 캡처 분석 | 없음 | 있음 |
| 동영상 | 없음 | 있음 |
| AI | 제목·구성 추천 | + 상품 분석·고객 분석·장점 정리 |
| 작업파일 저장/불러오기 | 없음 (자동저장은 있음) | 있음 |
| 스마트스토어 체크 | 없음 | 있음 |
| 이미지 출력 | 전체 이미지 | + 분할 JPG · ZIP |

---

**8. 저장소를 옮긴 이유**
예전에는 간단한 저장소(localStorage)를 썼는데 5MB 정도가 한계라
사진을 몇 장만 넣어도 자동저장이 실패했다. 사진은 글자로 바꿔 담기 때문에 원본보다 1.3배쯤 커진다.
→ 넉넉한 저장소(IndexedDB)로 옮겼다. 값을 글로 바꾸지 않고 그대로 넣어서 더 빠르기도 하다.
예전 자리에 저장돼 있던 작업은 **처음 열 때 자동으로 새 저장소로 옮기고** 예전 자리는 비운다.
저장소를 못 쓰는 환경(사생활 보호 모드 등)에서는 예전 방식으로 물러나고, 그것마저 안 되면
"사진을 줄이거나 작업파일로 받아두세요" 라고 화면에 알린다. 서버로는 보내지 않는다.

**19. 글꼴은 고른 것만 받아온다**
다섯 개를 한꺼번에 받으면 느려진다. 고른 글꼴만 그때그때 `<link>` 나 `@font-face` 로 넣는다.
굵기도 보통(400)·굵게(700) 두 가지만 받는다. 못 받아와도 대체 글꼴로 보이고 앱은 그대로 돌아간다.
이미지로 저장할 때는 글꼴을 **함께 넣어야** 화면과 같은 글씨로 나온다.
그래서 `getFontEmbedCSS` 로 챙기되 12초 제한을 두고, 실패하면 글꼴 없이라도 이미지를 만든다.
(예전에 글꼴을 받아오다 멈춘 적이 있어 시간 제한을 꼭 둔다)

**20. 진행 표시는 실제로 하는 일만 적는다**
예전에는 '문구 작성 중 → 사진 배치 중 → 디자인 적용 중' 을 시간차로 보여줬는데,
실제로는 한 번에 계산이 끝나므로 **보여주기용 단계**였다.
지금은 실제로 일어나는 세 가지(정보 정리 → 구성·문구 만들기 → 편집 항목에 넣기)만 적고,
"진짜 AI 서버에는 아직 연결되어 있지 않다" 고 화면에 그대로 밝힌다.

**17. 촬영상품이 ① 로 넘어가지 않던 원인**
촬영상품을 고르면 `shoot.productName` 만 바뀌고 `product` 는 손대지 않았다.
게다가 새 프로젝트에 **커피 샘플**이 들어 있어서, 가족사진을 고른 뒤 편집 화면으로 가면
커피 상품이 그대로 보였다.
→ 촬영상품을 고르는 순간 `studioDraft()` 로 ① 의 **빈 칸만** 채우도록 했다.
  (이미 적어두신 값은 건드리지 않는다)
→ 커피 샘플은 통째로 지웠다. 사진관 도구에 다른 업종 샘플을 두지 않는다.

**18. AI 는 따로 결과물을 만들지 않는다**
예전에는 AI 전용 화면이 따로 있어서 '만드는 곳'과 '고치는 곳'이 갈라져 있었다.
이제 AI 는 ⑤ 패널 안에서 돌고, 결과는 곧바로 ①~④ 의 값이 된다.
만드는 동안에도 왼쪽 미리보기가 그대로 보이고, 만든 것부터 순서대로 반영된다.
범위를 고를 수 있어서 기본값('비어 있는 내용만 채우기')으로는 적어둔 글이 절대 바뀌지 않는다.
순서만은 추천대로 맞춘다 — 순서를 바꾸는 것은 글을 지우는 일이 아니라서 안전하다.

**15. 캡처 속 글자는 읽는 척하지 않는다**
사진으로 올린 가격표에서 글자를 읽으려면 OCR 이 필요하다.
한글 OCR 을 붙이면 가격을 **틀리게 읽을 수 있고**, 이 프로그램에서 가장 중요한 것은 정보 정확성이다.
틀린 가격이 상세페이지에 박히는 것보다 못 읽는 편이 낫다.
→ 대신 **글을 복사해 붙여넣는 길**을 열었다. 스마트플레이스·가격표의 글을 그대로 붙이면
상호·주소·전화·영업시간·휴무·가격·포함사항·추가비용·혜택·기간·콘셉트를 찾아낸다.
화면에도 "캡처 사진 속 글자는 아직 읽지 못합니다" 라고 그대로 적었다.

**16. 없는 값은 채우지 않고 섹션을 숨긴다**
가격·이벤트·혜택·후기처럼 **사실이 필요한 섹션**은 확인된 값이 없으면 숨긴다.
빈 칸이나 "여기에 적어주세요" 같은 안내문이 최종 이미지에 남으면 안 되기 때문이다.
장점·추천·촬영과정·준비사항은 일반적인 내용으로 채우되,
확인 화면에서 "일반적인 내용으로 채워뒀습니다. 사진관에 맞게 고쳐주세요" 라고 알린다.

**13. 내용과 모양을 완전히 나눈다**
섹션마다 여러 모양(템플릿)을 두되, 모양은 **그리는 방법일 뿐** 데이터에 손대지 않는다.
`components/preview/sections.tsx` 가 모양을, `templates.ts` 가 목록을 맡는다.
그래서 모양을 몇 번을 바꿔도 적어둔 글·사진·가격이 사라지지 않는다.
사진을 직접 배치하는 모양은 `ownsPhotos()` 로 표시해 아래 공통 사진 칸을 두 번 그리지 않게 한다.

**14. 한 섹션만 고치면 그 섹션만 바뀐다**
`regenerateMenu()` 는 고른 메뉴 하나만 만진다. 다른 섹션은 함수가 아예 건드리지 않는다.
되는지 안 되는지는 **상태를 바꾸기 전에** 미리 따진다 —
상태 갱신 함수 안에서 바깥 값을 읽으면 아직 실행 전이라 늘 틀린 값이 나온다. (1번 문제와 같은 원인)

**11. 원본 인물 보호가 디자인보다 우선이다**
사진관은 실제 고객 얼굴을 다룬다. 그래서 이 프로그램에는
**사진을 새로 만들어내는 기능을 넣지 않는다.** 얼굴·표정·머리·옷·체형은 어떤 경우에도 건드리지 않는다.
하는 일은 고르기·크기·위치·배치뿐이다. 규칙은 `docs/PHOTO-POLICY.md` 에 적어두고
첫 화면에도 사용자가 볼 수 있게 표시했다.

**12. 틀리면 안 되는 값은 지어내지 않는다**
가격·전화번호·주소·포함사항은 프로그램이 확정하지 않는다.
모르면 비워두고 "확인이 필요합니다" 라고 말한다.
캡처에서 글자를 읽는 기능(OCR)은 아직 연결하지 않았으므로 읽은 척하지 않는다.

**10. 올린 사진은 알맞은 크기로 줄여서 담는다**
요즘 휴대폰 사진은 4000px 이 넘는데, 상세페이지는 860px 로 만들어 2배로 뽑으므로
1720px 보다 큰 부분은 화면에도 출력에도 쓰이지 않는다. 저장 공간만 먹고 출력도 느려진다.
→ 올릴 때 `maxStoredWidth 1720 · maxStoredHeight 3600` 안으로 줄인다. (`config/smartstore.ts`)

지키는 규칙:
- 가로·세로를 **같은 비율로** 줄인다 → 찌그러지지 않는다
- **잘라내지 않는다** → 얼굴이나 상품이 임의로 잘릴 일이 없다 (자르기는 지금처럼 보여줄 때만 한다)
- 원래 작은 사진은 **절대 키우지 않는다** (억지로 키우면 흐려진다)
- 줄였는데 용량이 되레 커지면 원본을 쓴다
- 투명한 부분이 있는 사진은 PNG 그대로 둔다 (JPG 로 바꾸면 배경이 까맣게 된다)
- 한 번에 확 줄이면 거칠어지므로 **절반씩 여러 번** 줄인다
- 무슨 일이 생겨도 원본을 쓴다 — 사진을 잃지 않는다

세로 최대값을 3600 으로 넉넉히 둔 이유: 이 값이 낮으면 흔한 휴대폰 세로 사진(9:16)이
가로까지 같이 줄어들어(1720 → 1349) 출력이 흐려진다. 실제로 2400 으로 두었다가 확인하고 올렸다.

**9. '강렬한' 스타일은 뺐다**
사용자 요청으로 간편 스타일이 6종(깔끔한/고급스러운/감성적인/따뜻한/미니멀/밝은)이 됐다.
예전에 '강렬한'으로 저장해 둔 작업파일도 열린다 — 색·글씨·여백은 만든 그대로 두고 스타일 이름만 바꾼다.
참고 캡처가 어두운 화면일 때 추천하던 스타일도 '미니멀'로 바꿨다. (배경색 자체는 캡처에서 읽은 값을 쓴다)

---

## 작업 중 실제로 겪고 고친 문제

1. **실행취소가 동작하지 않던 문제**
   상태 갱신 함수 안에서 바깥 ref(past/future)를 바꾸고 있었는데,
   React 개발 모드가 갱신 함수를 두 번 호출해 기록이 어긋났다.
   → `useReducer` 로 바꾸고 past/future/present 를 **하나의 상태**로 두어 순수 함수로만 변경.

2. **미리보기가 통째로 죽던 문제** (`PhotoBlock is not defined`)
   컴포넌트 파일이 컴포넌트가 아닌 함수(`formatWon` 등)도 내보내 Fast Refresh 가 깨졌다.
   → `utils/format.ts` 로 분리.

3. **사진이 한 장도 안 올라가던 문제**
   `input.value = ''` 를 먼저 실행해 `FileList` 가 비워진 뒤에 읽고 있었다.
   → 파일을 배열로 먼저 복사한 다음 비우도록 수정.

4. **이미지 출력이 멈추던 문제**
   `html-to-image` 의 `cacheBust` 가 사진의 `data:` 주소 뒤에 값을 붙여 주소를 망가뜨렸다.
   → `cacheBust: false`, `fontEmbedCSS: ''` 로 변경하고 60초 시간 제한을 걸어
   멈추면 "사진 수나 크기를 줄여보세요" 라고 알려주도록 했다.

5. 빌드 설정: `tsconfig.node.json` 의 `composite` + `noEmit` 충돌 → `emitDeclarationOnly` 로 해결.

6. **참고 캡처에서 글자색을 사진 색으로 잘못 고르던 문제**
   화면 전체에서 어두운 색을 찾다 보니 큰 사진 덩어리의 색이 글자색으로 뽑혔다.
   → 글자처럼 보이는 줄(가로로 조금만 채워진 줄)에서만 글자색을 찾도록 바꿨다.

7. **참고 캡처의 본문 글씨 크기가 항상 최대값으로 나오던 문제**
   사진을 줄여서 보면 가까이 붙은 글줄이 한 덩어리로 뭉쳐 높이가 부풀었다.
   → 본문 크기는 **짐작하지 않기로** 했고, 제목은 위쪽에서 처음 만나는
   충분히 얇은 글줄 하나만 보도록 바꿨다. (뭉친 덩어리는 건너뛴다)

8. **저장소를 옮긴 뒤 아무것도 저장되지 않던 문제**
   불러오기에 "화면이 사라지면 그만두기" 장치를 달았는데,
   React 개발 모드가 화면을 한 번 붙였다 떼었다 다시 붙이기 때문에
   불러오기가 스스로 취소돼 버려 그 뒤로 저장도 복원도 되지 않았다.
   → 그 장치를 없애고 "한 번만 불러오기" 표시만 남겼다.
   (1번 문제와 원인이 같다 — 개발 모드의 이중 실행)

---

## localhost 테스트 결과 (MASTER 최종 테스트 흐름 전 구간)

| 항목 | 결과 |
|---|---|
| 새 프로젝트 | 통과 |
| 상품명·가격 입력 → 미리보기 즉시 반영 | 통과 (할인율 자동 계산 포함) |
| 사진 여러 장 등록 (세로/가로/정사각형/저해상도) | 통과 — 4장, 비율 자동 인식 |
| 사진 자동맞춤 | 통과 — 찌그러짐 없음, 저해상도 안내 표시 |
| 대표사진 지정 | 통과 |
| 메뉴 추가 / 복제 / 숨기기 / 삭제 | 통과 (후보 22종) |
| 순서변경 (드래그) | 통과 |
| 디자인 변경 (프리셋 7종 + 세부 설정) | 통과 |
| PC 미리보기 (860px) / 모바일 미리보기 (390px) | 통과 |
| 좌우 폭 드래그 조절 · 편집도구 숨기기 | 통과 |
| 미리보기 메뉴 클릭 → 해당 편집카드 열림 | 통과 |
| 자동저장 | 통과 |
| 새로고침 → 작업 복원 | 통과 (사진 4장·디자인까지) |
| 프로젝트 저장 → 다시 불러오기 | 통과 (180KB 작업파일) |
| 실행취소 / 다시하기 | 통과 |
| 스마트스토어 체크 | 통과 (✓ 문제없음 / ⚠ 수정 권장 N개) |
| 전체 이미지 생성 | 통과 — JPG, 1720px 폭 (860 × 2배 선명) |
| 분할 JPG | 통과 — 메뉴 사이 여백에서 자름 |
| ZIP 다운로드 | 통과 |
| AI 제목·메뉴 추천 → 선택 적용 | 통과 |
| SAY PROMPTER 요청문 생성 (GPT/Claude/Codex) | 통과 |
| 모바일 (375px) | 통과 — 세로 배치, 가로 스크롤 없음, 입력·미리보기 정상 |
| 빌드 | 통과 (`tsc -b && vite build` 오류 없음) |
| 콘솔 오류 | 없음 |

### 7차 작업 테스트 (localhost 실제 실행)

| 항목 | 결과 |
|---|---|
| 글꼴 5종 로드 | 통과 — 너비 측정으로 실제 적용 확인 (Pretendard 628 · Spoqa 661 · 기본 670 · 나눔고딕 684 · Gmarket 699) |
| 저장 이미지에 글꼴 반영 | 통과 — 같은 페이지를 Pretendard(551KB) / Gmarket(567KB) 로 저장해 결과가 다름 확인 |
| SAY PROMPTER 기본값 | 통과 — 기본형 |
| 선택지 3개 · Codex 없음 | 통과 |
| 기본형에 특정 AI 이름 없음 | 통과 |
| ChatGPT용 / Claude용 전환 | 통과 — 내용이 실제로 달라짐 |
| 유형 변경 후 적은 글 유지 | 통과 |
| 복사 버튼 | **확인 불가** — 스크립트 조작이라 브라우저가 클립보드를 막음 (NotAllowedError). 사람이 직접 누르면 동작하는 자리 |
| 사진 '사용하지 않음' | 통과 — 목록에 추가됨 |
| 실시간 반영 · 실행취소 · 다시하기 · 자동저장 | 통과 |
| 스마트스토어 체크 | 통과 — ✓ 문제없음 |
| 분할 JPG 4장 · ZIP 567KB | 통과 |
| 새로고침 복원 (글꼴 포함) | 통과 — GmarketSans 유지 |
| 모바일 375px | 통과 — 가로 스크롤 없음, 글꼴 카드·입력창 안 잘림 |
| 빌드 · 콘솔 오류 | 없음 |

**Gmarket Sans 주소가 404 였던 문제**를 찾아 고쳤다.
제공되는 스타일시트가 없어서 `@font-face` 를 직접 적고 필요한 굵기(보통·굵게)만 받도록 했다.

### 6차 작업 테스트 (localhost 실제 실행 · 요청하신 18항목)

| # | 항목 | 결과 |
|---|---|---|
| 1~3 | 첫 화면에 AI 전용 화면 없음 · 좌우 함께 표시 | 통과 |
| 4~5 | 상단 [AI로 만들기] → ⑤ 패널 열림 (이동 없음) | 통과 |
| 6~7 | 가족사진 선택 → ① 에 상품명·촬영 종류·한 줄 소개·추천 대상 반영 | 통과 |
| 8 | 커피 샘플 없음 | 통과 |
| 9 | 상품명 수정 → 미리보기 즉시 반영 | 통과 |
| 10 | 실행취소 / 다시하기 | 통과 |
| 11 | 생성 범위 6가지 · 기본 '비어 있는 내용만 채우기' · 전체 선택 시 경고 | 통과 |
| 11 | 생성 — 진행 7단계 순서대로, 페이지 이동 없음, 12섹션 | 통과 |
| 12 | AI 결과를 ③ 에서 다시 수정 → 미리보기 반영 | 통과 |
| 13 | PC·모바일 미리보기 | 통과 |
| 14 | 간편 ↔ 상세 편집 전환 후 데이터 유지 | 통과 (12섹션·상품명 그대로) |
| 15 | 새로고침 후 자동저장 복원 | 통과 |
| 16 | 작업파일 저장 → 새로 시작 → 불러오기 | 통과 |
| 17 | 전체 이미지 1720×9512 · 분할 4장 · ZIP 554KB | 통과 |
| 18 | 빌드 오류 · 콘솔 오류 | 없음 |
| — | 모바일 375px (미리보기 위 / 편집 아래, 가로 스크롤 없음, 칩 줄바꿈, 버튼 안 잘림) | 통과 |
| — | 디자인 스타일 카드 6종 · 고급스러운 적용 시 배경 변경 확인 | 통과 |

작업 중 고친 것:
- 'AI 자동 작성'에서 만든 뒤 **예약·문의가 한가운데** 남던 문제 → 순서만 추천대로 정렬
- 이벤트·가격·혜택처럼 **따로 저장되는 값**으로 채워진 섹션을 검수가 "비어 있다"고 잘못 지적하던 문제

### 5차 작업 3단계 테스트 (localhost 실제 실행 · 가족사진 예시)

촬영사진 8장 + 스마트플레이스/가격표 글 붙여넣기 + 촬영상품 '가족사진'.

| 항목 | 결과 |
|---|---|
| 정보 읽기 | 통과 — **16항목 전부** (상호·지역·주소·전화·영업시간·휴무·촬영상품·정상가·이벤트가·기준인원·포함사항·추가비용·혜택·기간·콘셉트·예약링크) |
| 값 고치기 + [확인하고 적용] | 통과 |
| 사진 역할 자동 추천 | 통과 — 대표 1장, 세로 4장 → 콘셉트, 나머지 갤러리 |
| 대표사진 한 장만 | 통과 (고친 뒤) |
| 실제 정보 반영 | 통과 — 가격 250,000 → 189,000 (24%), 4인 기준, 포함 3개, 혜택 3개, 기간 2026.01.01~02.28 |
| 템플릿 자동 선택 | 통과 — 이벤트 '사진+가격', 가격 '패키지 카드', 콘셉트 '사진 왼쪽/설명 오른쪽' |
| 샘플 제거 | 통과 — 결과에 샘플 글 없음, 빈 섹션 없음 |
| 내용 없는 섹션 | 통과 — 후기만 숨김, 화면에 이유 표시 |
| 만든 뒤 확인 | 통과 — [이대로 사용] [부분 수정] [전체 다시 만들기] |
| 부분 재생성 (문구만) | 통과 — 다른 섹션 안 바뀜 |
| 디자인 변경 (혜택 → 카드) | 통과 — 내용 유지 |
| 직접 수정 | 통과 — 고친 글이 바로 미리보기에 반영 |
| 스마트스토어 체크 | 통과 — **✓ 문제없음** |
| 전체 이미지 출력 | 통과 — 1720×12428, 0.71MB, 2.5초 |
| 빌드 · 콘솔 오류 | 없음 |

작업 중 고친 것 6가지:
- 대표사진이 **두 장**이 되던 문제 (첫 사진 규칙과 추천이 겹쳤다) → 한 장만 남기도록 정리
- '영업시간 10:00~19:00' 에서 **'영업시간 10:' 까지 떼어내던** 이름표 제거 규칙 → 숫자가 섞이면 이름표로 보지 않는다
- 휴무일이 영업시간과 한 줄에 있으면 통째로 들어가던 문제 → 휴무 쪽 조각만 남긴다
- 콘셉트 첫 항목에 '콘셉트:' 가 붙던 문제
- 확인 화면의 구성이 **실제 결과와 달랐던** 문제 → 읽은 값을 넣어본 복사본으로 미리 짠다
- 혜택을 [글 수정]으로 고쳐도 화면에 안 바뀌던 문제 → 섹션이 읽어온 목록보다 **적어둔 줄을 먼저** 보게
- '4인 기준 기준' 겹침, 사진관인데 '구매하러 가기' → '예약하러 가기'
- 가격표 '포함' 줄들의 React 열쇠가 겹치던 경고

### 5차 작업 2단계 테스트 (localhost 실제 실행 · 가족사진 예시)

사진 7장 + 촬영상품 '가족사진' + "광명 가족사진, 따뜻하고 고급스럽게" 로 확인.

| 항목 | 결과 |
|---|---|
| 자동 생성 | 통과 — 13개 섹션, 사진 7장 자동 배치 (메인1·이벤트1·콘셉트2·갤러리3) |
| 글 수정 (혜택 3개 입력) | 통과 — 미리보기에 01/02/03 으로 바로 반영 |
| 디자인 변경 — 혜택 5가지 모양 | 통과 — 번호/카드/사진+설명/아이콘/세로 스토리, **내용 유지, 다른 섹션 그대로** |
| 디자인 변경 — 이벤트4·가격4·콘셉트5·장점4·추천4·갤러리4 | 통과 — 모두 내용 유지 |
| 부분 재생성 제목만 / 문구만 / 사진 배치만 / 디자인만 | 통과 — 4가지 모두 **다른 섹션 안 바뀜** |
| 전체 다시 만들기 (내용 그대로, 디자인만) | 통과 — 글·사진 7장 그대로 |
| 스마트스토어 체크 | 통과 — ✓ 큰 문제 없음 |
| 전체 이미지 출력 | 통과 — 1720×11096, 0.77MB, 2.9초 |
| 분할 JPG | 통과 — 5장 (1720×2978 등), 메뉴 사이에서 잘림 |
| 모바일 375px | 통과 — 섹션 도구 4개 탭 다 보이고 가로 스크롤 없음 |
| 빌드 · 콘솔 오류 | 없음 |

작업 중 고친 것 2가지:
- **부분 재생성이 성공했는데 "만들 수 없다"고 잘못 알리던 문제** — 상태를 바꾸는 함수 안에서 결과를 읽고 있었다.
  미리 따져보고 알리도록 수정. (실행취소가 안 되던 것과 같은 원인)
- **React 경고 (여백 줄임말과 낱개 값 섞임)** — 본문 기본 여백을 낱개 값으로 바꿔 해결.

> ZIP 저장은 이번 회차에서 끝까지 확인하지 못했다. 미리보기 창이 화면에서 가려지면
> 브라우저가 이미지 그리기를 멈춰 출력이 끝나지 않기 때문이다. (프로그램 문제가 아니라 시험 환경 문제)
> 같은 코드를 쓰는 분할 JPG 는 통과했고, ZIP 코드는 이번에 건드리지 않았다.

### 5차 작업 테스트 (localhost 실제 실행)

| 항목 | 결과 |
|---|---|
| 첫 화면 | 통과 — STEP 1·2·3 + [AI로 상세페이지 만들기], 편집도구는 숨겨짐 |
| 기본 촬영상품 9종 | 통과 |
| 촬영상품 추가 ('웨딩사진') | 통과 — 목록에 붙고 바로 선택됨 |
| 사진 6장 올리기 → 자동 최적화 | 통과 |
| 사진관 정보 저장 | 통과 — 상호·지역·전화·영업시간·휴무 저장, 다음에도 다시 씀 |
| 확인 화면 | 통과 — 상호/지역/전화/영업시간/촬영상품 확인 목록 + "가격을 아직 확인하지 못했습니다 (임의로 만들지 않았습니다)" |
| 검색 제목 후보 | 통과 — 광명 가족사진 촬영 / 예약 안내 / 오늘사진관 가족사진 / 스튜디오 |
| 자동 생성 결과 | 통과 — 12개 섹션, 사진 6장 자동 배치 (메인1·콘셉트2·갤러리3) |
| 한 줄 요청 반영 | 통과 — "가격과 혜택이 눈에 띄게" → 이벤트·혜택·가격이 앞으로 이동 |
| 용어 변경 | 통과 — 촬영상품 정보 / 사진·영상 / 상세페이지 구성 / AI 자동 작성 / 완성·저장 |
| 새로고침 복원 | 통과 — 편집 화면으로 복원 |
| 스마트스토어 체크 | 통과 — ✓ 큰 문제 없음 |
| 전체 이미지 출력 | 통과 — 1720×13540, 0.87MB |
| 실행취소 / 다시하기 | 통과 |
| 모바일 375px | 통과 — 가로 스크롤 없음 |
| 빌드 · 콘솔 오류 | 없음 |

작업 중 고친 것 2가지:
- 사진관 정보 입력칸에서 **한 글자만 치면 커서가 튕기던 문제** — 입력칸 컴포넌트를 부모 함수 안에서 만들고 있었다. 밖으로 빼서 해결.
- 촬영 과정·준비사항처럼 **줄 목록으로 채우는 메뉴를 검수가 "비어 있다"고 잘못 지적**하던 문제 — `lines` 도 내용으로 세도록 수정.

### 4차 작업 테스트 (localhost 실제 실행)

사진 8장(가로·세로·정사각형·파노라마·극단 세로·작은 사진 2장·투명 PNG)을 한 번에 올려서 확인.

| 올린 사진 | 저장된 크기 | 비율 | 용량 |
|---|---|---|---|
| 4032×3024 (가로) | 1720×1290 | 그대로 | 1932KB → 271KB |
| 3024×5376 (휴대폰 세로 9:16) | 1720×3058 | 그대로 | 2592KB → 595KB |
| 3000×3000 (정사각형) | 1720×1720 | 그대로 | 1444KB → 331KB |
| 5000×1200 (파노라마) | 1720×413 | 그대로 | 977KB → 98KB |
| 2000×6000 (1:3 극단) | 1200×3600 | 그대로 | 1943KB → 479KB |
| 900×1400 (작음) | **그대로** | — | 222KB (건드리지 않음) |
| 500×500 (아주 작음) | **그대로** | — | 49KB (건드리지 않음) |
| 2400×1800 투명 PNG | 1720×1290 | 그대로 | 94KB → 53KB (투명 유지) |

| 항목 | 결과 |
|---|---|
| 전체 용량 | 9.04MB → **2.05MB** (77% 줄어듦) |
| 비율 유지 | 통과 — 원본 대비 오차 0.05% 이내, 찌그러짐 없음 |
| 작은 사진 확대 | 없음 — 두 장 모두 원본 그대로 |
| 잘림 | 없음 — 크기만 줄인다 |
| 투명 PNG | 통과 — PNG 로 남고 투명 픽셀 220만 개 유지 |
| 새로고침 후 복원 | 통과 — 8장 전부, 가로/세로/정사각형 판정도 그대로 |
| 사진 자동맞춤 | 통과 — 4:3 사진이 780×585 로 정확히 표시 (찌그러짐 없음) |
| 전체 이미지 출력 | 통과 — 1720×7388, 1.14MB, **0.8초** |
| 선명도 | 통과 — 24px 잔무늬가 출력물에서 살아 있음 (명암차 119/255) |
| 분할 JPG | 통과 — 2장 (1720×2152, 1720×5236) |
| ZIP | 통과 — 1172KB |
| 콘솔 오류 | 없음 |

> 출력이 한 번 시간 초과된 적이 있는데, 브라우저 **뒤쪽 탭**에서 돌려서 느려진 것이었다.
> 앞쪽 탭에서는 0.8초에 끝난다. 프로그램 문제가 아니다.

### 3차 작업 테스트 (localhost 실제 실행)

| 항목 | 결과 |
|---|---|
| 간편 스타일 | 통과 — 6종, '강렬한' 사라짐 |
| 없어진 스타일로 저장된 옛 작업 열기 | 통과 — 색(#111111 등) 그대로 유지, 화면 안 깨짐 |
| 큰 사진 12장(원본 18MB) 올리기 | 통과 — 경고 없음, '자동저장됨' |
| 저장된 용량 | 24.1MB — 예전 저장소(5MB)로는 불가능한 크기 |
| 새로고침 후 복원 | 통과 — 사진 12장 그대로 |
| 예전 저장소 → 새 저장소 자동 옮김 | 통과 — 옮긴 뒤 예전 자리는 비워짐 |
| 실행취소 / 다시하기 | 통과 (이어하기 직후 실행취소가 샘플로 돌아가지 않음) |
| 작업파일 저장 → 새 프로젝트 → 다시 불러오기 | 통과 — 사진 12장 복원 |
| 스마트스토어 체크 · 전체 이미지 출력 | 통과 — 1720×3616 |
| 콘솔 오류 | 없음 |

### 2차 작업 테스트 (localhost 실제 실행)

| 항목 | 결과 |
|---|---|
| 제작 방식 고르기 (첫 화면) | 통과 — 3가지 + 샘플 둘러보기, 한 번 고르면 다시 묻지 않음 |
| SAY PROMPTER로 만들기 선택 | 통과 — 새 프로젝트 + ⑥단계 자동 열림 |
| 대문 사진 모양 | 통과 — 가로형 585px / 정사각형 780px / 세로형 975px (폭 780 기준) |
| 대문 모양 자동 추천 | 통과 — 세로 사진에 "세로형이 잘 어울려요" 안내 |
| 참고 캡처 분석 (밝은 고급형) | 통과 — 배경 #f7f5f2 · 색 #8b7355 / 글자 #2b2620 · 구간 7 · 여백 108px · 가로형 · 제목 35px · 아래 버튼 있음 → '고급스러운' 추천 |
| 참고 캡처 분석 (어두운 강렬형) | 통과 — 배경 #111111 · 색 #ff4d2d · 구간 4 · 여백 61px · 세로형 → '강렬한' 추천 |
| 참고 캡처 적용 / 실행취소 | 통과 — 상품명·사진·메뉴는 그대로, 되돌리기 정상 |
| LITE 노출 범위 | 통과 — 메뉴 10종 · 색상만 · 동영상 없음 · 참고 캡처 없음 · 전체 이미지 저장만 |
| PRO 노출 범위 | 통과 — 메뉴 22종 · 세부설정 5종 · 동영상 · 참고 캡처 · 체크 · 분할 · ZIP |
| LITE/PRO 새로고침 후 유지 | 통과 |
| 출력 회귀 (대문 모양 적용 후) | 통과 — 1720px 폭, 2장 분할, ZIP 정상 |
| SAY PROMPTER 요청문 | 통과 — 대문 모양과 참고 캡처 내용까지 포함 |
| 모바일 375px | 통과 — 가로 스크롤 없음, 새 화면(제작 방식·대문 모양·참고 캡처) 모두 들어감 |
| 빌드 / 콘솔 | 통과 — 오류 없음 |

---

## 남은 것 / 다음에 할 만한 것

- **실제 AI 연결**: `src/services/ai/` 에 provider 하나만 추가하면 된다.
  API 키는 반드시 서버(또는 서버리스 함수)에 두고, 화면 코드에 넣지 말 것.
- **SAY PROMPTER 실제 연동**: 지금은 요청문 생성까지. 별도 프로젝트가 준비되면
  `services/prompter/` 에 연결 어댑터를 추가한다.
- **참고 캡처 분석 넓히기**: 지금은 색·여백·구간·사진 비율·아래 버튼까지 읽는다.
  메뉴 순서(어떤 내용이 먼저 오는지)까지 읽으려면 글자를 읽어야 해서 아직 하지 않았다.
- **사진 최적화 더 하기**: 지금은 크기와 품질만 줄인다. 형식을 WebP 로 바꾸면 더 줄일 수 있지만
  아주 오래된 브라우저에서 안 보일 수 있어 하지 않았다.
- **사진이 아주 많을 때**: 넉넉한 저장소로 옮겨서 18MB(사진 12장)까지 확인했다.
  브라우저가 주는 공간은 컴퓨터마다 다르므로(보통 수백 MB) 그보다 더 넣으면 여전히 실패할 수 있다.
  그때는 화면 위에 안내가 뜨고 작업파일로 저장하도록 유도한다.
  더 줄이려면 사진을 올릴 때 크기를 줄여 담는 방법을 검토.
- LITE/PRO 노출 범위는 위 표대로 나눴다. 더 조정하려면 각 단계의 `useEdition()` 부분만 고치면 된다.

---

## 이어서 작업할 때

| 하고 싶은 것 | 고칠 곳 |
|---|---|
| 스마트스토어 규격 변경 | `src/config/smartstore.ts` |
| 메뉴 종류 추가 | `src/types/defaults.ts` 의 `MENU_CATALOG` + `DetailPage.tsx` 의 `MenuBody` |
| 디자인 프리셋 추가 | `src/types/defaults.ts` 의 `DESIGN_PRESETS` |
| 사진 맞춤 규칙 | `src/utils/image.ts` |
| 링크·붙여넣기에서 읽는 규칙 | `src/services/read/parseInfo.ts` |
| 읽은 값이 들어가는 자리 | `src/services/read/applyFields.ts` |
| 페이지 열어오는 방법 | `collector/collect.js` |
| 수집 서비스 자리(포트) | `collector.config.json` (한 곳만) |
| 검수 항목 추가 | `src/services/export/checkStore.ts` |
| 분할 위치 규칙 | `src/services/export/exportImage.ts` 의 `pickCuts` |
| AI 추천 내용 | `src/services/ai/localPlanner.ts` |
| 참고 캡처에서 읽는 항목 | `src/services/reference/analyzeCapture.ts` |
| LITE/PRO 노출 범위 | 각 단계의 `useEdition()` 쓰는 곳 |
| 대문 사진 모양 비율 | `src/utils/image.ts` 의 `heroBoxHeight` |
| 저장 방식 | `src/services/storage/db.ts` · `local.ts` |
| 올린 사진 줄이는 크기·품질 | `src/config/smartstore.ts` 의 `maxStoredWidth` 등 |
| 사진 줄이는 방법 | `src/utils/image.ts` 의 `fitForPage` |

- 화면에 개발 용어(object-fit, container, JSON 등)를 쓰지 않는다.
- 사용자가 입력한 내용을 허락 없이 지우거나 덮어쓰지 않는다.

## 2026-09-15 — 공개 주소 배포 (링크 가져오기 반영)

- `npm run build` → 공개 주소(cloudflared)에 반영 완료. 화면 파일 이름이 로컬·5190·공개 주소 3곳 모두 일치하는 것을 확인.
- **공개 주소에서는 링크 가져오기가 안 되는 문제를 솔직하게 알리도록 고침.**
  - 공개 주소로 열면 수집 서비스(collector)가 없다. 그런데 정적 서버가 `/api/*` 요청에도 화면 파일을 200으로 돌려줘서
    "연결됐다"고 착각할 수 있었다.
  - `isLocalHost()` 추가 — 사진관 컴퓨터에서 연 화면인지 구분.
  - `isJson(res)` 추가 — 돌아온 답이 수집 서비스가 보낸 것이 맞는지 확인. 화면 파일이 대신 온 경우는 못 닿은 것으로 본다.
  - `collectorAlive()` 는 공개 주소에서 항상 false.
  - 공개 주소로 열면 ③ 단계 위에 안내문이 먼저 뜨고, 눌러도 "사진관 컴퓨터에서 연 화면에서만 됩니다" 라고 알린다.
    붙여넣기 읽기는 공개 주소에서도 그대로 된다.
- 확인한 것: 공개 주소에서 안내문 표시 + 눌렀을 때 솔직한 안내 + 작업 내용 보존 / 로컬에서는 안내문 없음 + example.com 실제 읽기 성공.

## 2026-09-15 (2) — 공개 주소에서도 링크 가져오기가 되도록

문제: 공개 주소로 열면 링크 가져오기가 아예 막혀 있어서, 배포해도 달라진 게 없었다.

- 중계 서버(scratchpad/serve-dist.mjs)가 `/api/health` 와 `/api/import-url` **두 주소만** 이 컴퓨터의
  수집 서비스(5199)로 넘기도록 했다. 그 밖의 `/api` 주소는 404 로 막는다.
  (화면 파일을 대신 돌려주던 문제도 같이 없어졌다)
- 수집 서비스가 꺼져 있으면 중계 서버가 "꺼져 있다"고 그대로 알린다. 되는 척하지 않는다.
- 화면 쪽은 주소로 넘겨짚던 방식(isLocalHost)을 버리고, **실제로 켜져 있는지 확인**해서 안내한다.
  켜져 있으면 안내문이 뜨지 않고, 꺼져 있으면 켜달라고 알린다.
- 확인: 공개 주소에서 example.com 읽기 성공 / 켜짐 상태에서 경고문 없음 / 정해둔 것 밖의 /api 는 404.
- 주의: 이 통로는 사진관 컴퓨터의 수집 서비스를 인터넷에서 쓸 수 있게 열어준다.
  공개 주소를 아는 사람만 쓸 수 있고, 수집 서비스를 끄면 바로 닫힌다.

## 2026-09-15 (3) — 실제 사진관 페이지로 '읽는 규칙' 다듬기

지금까지 `parseStudioText` 는 **사람이 손으로 정리해 붙여넣은 글**로만 확인했었다.
실제 웹페이지에서 긁어온 날것의 글로 처음 돌려보니 **상호·가격·주소·휴무일이 거의 다 틀렸다.**

확인에 쓴 실제 페이지 (임시 폴더에 자료로 고정해두고 고치기 전/후를 비교했다):
수피아스튜디오 블로그 · 추억사진관 홈페이지 · 조아패밀리 홈페이지 · 조아패밀리 스마트플레이스

**고치기 전 17개 맞음 / 13개 틀림 → 고친 뒤 36개 전부 맞음**

### 읽는 규칙 (`src/services/read/parseInfo.ts`)

| 무엇 | 전 | 후 |
|---|---|---|
| 가격 `6만원` 표기 | **하나도 못 읽음** | 60,000 으로 읽음 (사진관 블로그 대부분이 이 표기) |
| 한 줄에 금액 두 개 | 앞의 것만 읽고 뒤는 버림 | 둘 다 읽음 — **이벤트가격이 아예 안 잡히던 문제** |
| 상호 | `STUDIO` (로고 글자), 인사말 문장 통째 | `조아패밀리 광명점`, `수피아스튜디오` |
| 주소 | 홍보문구(`광명/안양/시흥`)가 주소로 잡힘 | 행정구역 짜임새를 갖춘 것만 |
| 휴무일 | `…(매주 화,수 휴무) : 02-6408-2345` | `매주 화,수 휴무` |
| 포함사항 | 문장 속 '제공' 에 걸려 전화·링크까지 12줄 | 이름표 모양일 때만 모음 |
| `20,000` | `20` / `000원` 으로 쪼개짐 | 그대로 |
| 전화번호 | `0507-…` 이 `07-…` 로 잘림 | 안심번호도 제대로 |
| 영업시간 | `10:00~19:00` 한 가지만 | `오전 10시~오후 7시` 등도 |

- **그 밖에 찾은 가격** 항목을 새로 만들었다. 정상가·이벤트가로 가르지 못한 금액은
  **어느 것이 정상가인지 넘겨짚지 않고** 적힌 줄 그대로 받아둔다. (가격표가 통째로 들어온다)
  금액만 있는 줄은 바로 윗줄의 이름을 붙여 무슨 가격인지 알 수 있게 한다.

### 페이지 열어오는 쪽 (`collector/collect.js`)
- **홈페이지 글이 0자로 오던 문제** — 본문 자리가 있는데 속이 비어 있으면 그대로 빈 글을 줬다.
  이제 **글이 가장 많은 곳**을 고르고, 다 짧으면 페이지 전체를 쓴다. (추억사진관 0자 → 365자)
- **블로그 사진이 1장만 오던 문제** — 한 번에 맨 아래로 뛰어서 중간 사진이 안 붙었다.
  나눠서 내려가도록 고쳤다. (1장 → 6장)
- **스마트플레이스가 지도 메뉴 글자만 오던 문제** — 가게 화면(pcmap) 주소로 맞춰 열고,
  접혀 있는 영업시간을 펼친다. 이제 주소·영업시간·전화·**가격표**까지 읽는다.
  못 읽으면 읽은 척하지 않고 "스마트플레이스에서 읽지 못했습니다" 라고 그대로 알린다.
  (화면에도 스마트플레이스 주소를 넣으면 "못 읽을 수도 있다"고 미리 알려준다)

### 자리(포트) 부딪힘 — 꼭 알아둘 것
작업 중에 **5199 자리를 이 컴퓨터의 다른 프로그램 `BARODU Tools` 가 차지**하는 일이 생겼다.
그 프로그램도 `{"ok":true}` 를 돌려주기 때문에, 화면이 **"링크 가져오기가 켜져 있다"고 잘못 알렸다.**
되는 척하는 상태였다.

- 수집 서비스가 `service` 이름을 같이 보내고, 화면이 **그 이름까지 확인**하도록 고쳤다.
- 답이 우리 모양이 아니면 읽어온 척하지 않는다.
- 자리를 **5198** 로 옮겼다. `BARODU Tools` 는 건드리지 않았다.
- 자리 번호는 `collector.config.json` 한 곳에서만 관리한다 (화면·중계 서버가 같은 값을 읽는다).
- 자리가 이미 차 있으면 수집 서비스가 조용히 죽지 않고 **그대로 알리고 멈춘다.**
- 실패 이유가 80자에서 잘려 진짜 원인이 안 보이던 것도 고쳤다 (명령창에는 전체가 남는다).

### 확인한 것
| 항목 | 결과 |
|---|---|
| 읽는 규칙 36가지 (실제 4곳 + 화면의 붙여넣기 예시) | 전부 맞음 (전 17/30) |
| 링크로 가져오기 — 스마트플레이스·블로그·홈페이지 | 통과 (989자 / 1552자·사진6장 / 365자·사진7장) |
| 없는 가게 번호 | 통과 — "읽지 못했습니다" 로 솔직하게 |
| 화면에서 확인 → 적용 → 미리보기 반영 | 통과 (9개 항목, 상호·주소·전화·영업시간·가격표) |
| 실행취소 / 다시하기 | 통과 |
| 붙여넣기 읽기 (⑤) | 통과 — 13항목 전부 맞음, 오히려 정확해짐 |
| 스마트스토어 체크 | 통과 — 가격·상품명 없다고 솔직하게 알림 |
| 공개 주소에서 링크 가져오기 | 통과 |
| 빌드 · 콘솔 오류 | 없음 |

---

# ★ 이어받는 분께 — 2026-09-15 기준 현재 상태

## 작업 폴더 (중요)
**`C:\Users\notebook\Downloads\클로드\SAY-PAGE-MAKER-PRO`** 가 진짜 작업 폴더다.

`클로드\PAGE-MAKER-SMARTSTORE\SAY-PAGE-MAKER-PRO\` 에도 같은 파일이 있지만
그건 2026-09-15 에 복사해둔 **사본**이다. 여기서 고치면 갈라진다. 사본은 고치지 말 것.

`PAGE-MAKER`, `PAGE-MAKER-SMARTPLACE`, `PAGE-MAKER-SMARTSTORE` 는 **이름만 비슷한 별개 프로그램**이다.
MASTER.txt 9번대로 건드리지 않는다.

## 지금 켜져 있는 것
| 무엇 | 어디 | 켜는 법 |
|---|---|---|
| 편집 화면(개발) | http://localhost:5180 | `npm run dev` |
| 링크 가져오기 수집 서비스 | **127.0.0.1:5198** | `cd collector` → `npm start` |
| 공개용 중계 서버 | 127.0.0.1:5190 | scratchpad/serve-dist.mjs (dist 폴더를 내보냄 + /api 두 개를 수집 서비스로 넘김) |
| 공개 주소 | https://router-builders-trout-trees.trycloudflare.com | cloudflared quick tunnel |

공개 주소는 노트북을 끄면 사라지고, 다시 열면 주소가 바뀐다.
공개 주소에 새 내용을 반영하려면 `npm run build` 만 하면 된다 (중계 서버가 dist 를 그때그때 읽는다).

> **수집 서비스 자리가 5199 → 5198 로 바뀌었다.** (2026-09-15)
> 5199 는 이 컴퓨터의 다른 프로그램 **BARODU Tools** 가 쓰고 있다. 그 프로그램은 건드리지 않았다.
> 자리 번호는 이제 `collector.config.json` **한 곳**에서만 관리한다.
> 화면(vite.config.ts)과 중계 서버가 같은 값을 읽으므로, 바꿀 일이 생기면 그 파일만 고치면 된다.

## 되는 것
아이콘 메뉴 편집 화면 / AI 자동 만들기(규칙 기반) / 사진 자동 맞춤·최적화 / 섹션 템플릿 /
부분 다시 만들기 / 붙여넣기로 정보 읽기 / **링크로 가져오기** / 폰트 5종 / SAY PROMPTER 3종 /
내 작업 저장·열기·복제 / 완성 화면 / JPG·ZIP 내보내기 / 자동저장(IndexedDB)

## 아직 안 된 것 · 다음에 할 만한 것
- ~~실제 사진관 블로그로 링크 가져오기를 돌려 **읽는 규칙(parseInfo.ts) 다듬기**~~ → **2026-09-15 완료** (아래 참고)
- 바깥 AI 서버 연결은 여전히 없음. 글을 새로 지어내는 건 SAY PROMPTER 로 넘긴다 (가짜 연결 금지)
- **블로그 글의 목록형 포함사항** — '- 원목액자 제공' 처럼 이름표 없이 `-` 로만 적힌 줄은 아직 포함사항으로 안 모은다.
  넓게 잡으면 전화번호·링크까지 쓸어담아서 일부러 좁게 두었다. 필요하면 `collectLabelled` 을 손본다.
- **스마트플레이스는 네이버 화면이 바뀌면 또 깨질 수 있다.** 깨지면 `collect.js` 의 `naver-place` 만 고치면 된다.

## 지키는 규칙
- 올려주신 사진 속 인물은 원본 그대로. 얼굴·표정·머리·옷을 바꾸거나 새로 만들지 않는다. 크기와 위치만 맞춘다.
- 사용자가 이미 넣은 값이 기본 샘플보다 늘 우선.
- 안 되는 건 안 된다고 그대로 알린다. 되는 척하는 진행 표시 금지.
- 화면에 개발용어(JSON, container, aspect-ratio 등) 쓰지 않는다.
- 네이버 아이디·비밀번호를 이 화면에서 받지 않는다. 자동 발행·리뷰 수집·전체 크롤링 하지 않는다.
- 오류가 나도 자동저장·편집 중인 내용을 초기화하지 않는다.
