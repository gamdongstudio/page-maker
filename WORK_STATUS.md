# BARODU PAGE MAKER — 지금 상태

> **먼저 읽어주세요.** 지금은 더 만들지 않고, 직접 써보시는 단계입니다.

---

# ①~④ 4단계 흐름 개편 (2026-09-17) — 미리보기 주소에서 확인 중

**아직 공개 주소(barodu-page-maker.pages.dev)에는 올리지 않았습니다.** 확인용 주소에서 먼저 써보세요.

| 무엇 | 주소 |
|---|---|
| 확인용 주소 (새 흐름) | https://v2-flow.barodu-page-maker.pages.dev |
| 지금 공개 주소 (그대로) | https://barodu-page-maker.pages.dev |
| 작업 가지 | GitHub `v2-flow` (main 은 그대로) |

## 바뀐 흐름

처음 방문 → **[완성 예시 보기] / [내 상세페이지 만들기]**
→ ① 자료 준비 → ② 자동 추천 → ③ 보면서 고치기 → ④ 저장 → 저장 완료
→ (선택) 스마트스토어 등록 도우미 / 새 상세페이지

- **①** 자동입력·직접입력 구분 없음. 주소 여러 개 → 실패하면 내용 붙여넣기. 입력칸은 늘 보임.
  이미 적힌 값과 다르면 기존/새 자료를 보여주고 [추가][기존 내용 바꾸기][무시].
  연결 안내는 [글과 사진 가져오기]를 눌렀을 때만.
  사진은 보관함 하나 (출처 표시 · 제외 추천 · 교체 · 회전 · 대표 · 크게 보기 · 순서).
- **②** 버튼 하나로 글·사진·구성·고급스러운 디자인. 고친 뒤 다시 누르면 먼저 물어봄.
  만들기 전으로 되돌리기. ChatGPT로 문구 다듬기(복사 → 답 붙여넣기 → 적용). Claude 선택은 뺐음.
- **③** [내용][사진][구성][디자인] + 상세페이지 점검 (저장을 막지 않음).
  디자인은 스타일 · 글씨체 · 글자 크기 · 정렬만 먼저, 나머지는 [디자인 더 수정하기].
- **④** "상세페이지가 완성되었습니다" → 긴 이미지 / 여러 장 → 완료 화면. 준비 상태 %는 뺐음.
  상단 완성·저장 단추는 없애고 '자동저장됨'만.

## 이번에 고친 오류 (실제로 눌러보다 찾은 것)

1. 미리보기에서 눌러 연 구성 카드가 [닫기]로 안 닫힘 (카드 두 개 동시에 열림)
2. 저장 이미지에 "여기에 보입니다" 안내칸 · "예약 방법과 문의처를 적어주세요" 문장이 찍힘
3. **분할 저장에서 글이 반 잘리고 끝에 빈 장이 생김** — 그려진 그림을 보고 영역 단위로 자르게 바꿈
4. 맨 위 칸에 사진을 넣으면 전부 대문에 박혀 대표를 바꿔도 안 바뀜 · 대표를 지우면 대문이 빔
5. 미리보기 글자를 고치다 Ctrl+Z 를 누르면 작업 전체가 한 단계 되돌아감
6. 사진관 정보 보관을 작업 상태를 바꾸는 도중에 해서 화면 경고
7. 추천 직후 제목을 고르고 다시 추천하면 보호 질문 없이 덮어씀
8. 가격을 한 곳만 바꾸면 화면마다 다른 가격이 보일 수 있음 (상품정보·가격 안내·이벤트를 늘 함께)
9. 비슷한 구도의 다른 사진을 '같은 사진'으로 잘못 판단 (판정 강화)

## 시험 결과

| 시험 | 결과 |
|---|---|
| 첫 화면 · 완성 예시 · ① 자료 준비 | 33/33 |
| ② 자동 추천 · ③ 보면서 고치기 | 40/40 |
| ④ 저장 · 완료 · 등록 도우미 · 새 상세페이지 · 옛 작업파일 · 휴대폰 폭 | 18/18 |
| 실제 주소 2개(스마트플레이스+홈페이지) 합치기 → 자동 추천 | 8/8 |
| 예전 오류 점검 (구성·복제·숨기기·삭제·드래그·사진 교체·미리보기 도구) | 25/25 |
| 읽기 규칙 | 70/70 |
| 확인용 공개 주소에서 다시 | 51/51 |

분할 저장: 모든 장의 위·아래 끝에 글·사진이 걸치지 않음 · 빈 장 0 · 끝 공백은 원래 여백만.

## 지키는 것 (그대로)

- 옛 작업파일(.saypage)이 그대로 열림 — 새 칸은 모두 "없어도 되는 칸"
- 사진 속 인물은 원본 그대로 (크기·위치·방향만)
- 가격·전화번호는 지어내지 않음
- 예전 화면 파일(AiPanel·ImportUrl·StepPrompter·StepProduct·StepMedia·StepCheck)은 지우지 않고 남겨둠

## 2차 개선 후보

- 완성 예시 그림을 실제 촬영 사례 사진으로 바꾸기 (사용 허락 받은 사진이 생기면)
- 스마트스토어 기존 상품 전용 읽기 · 인스타그램 (준비 중 그대로)
- 사진이 아주 많을 때 '추천 사진 고르기' 화면

---


# 1차 공개본 최종 검수 (2026-09-16) — 통과

**서포터즈에게 보내셔도 됩니다.**

보낼 주소 → **https://barodu-page-maker.pages.dev**

이번 차례에는 **코드를 한 줄도 고치지 않았습니다.** 막는 문제가 없었기 때문입니다.
따라서 다시 만들지도, 다시 올리지도 않았습니다. (GitHub `main` 그대로, Pages 그대로)

## 공개 주소에서 실제로 눌러본 결과

사장님 컴퓨터의 크롬을 **임시 칸**으로 잠깐 열어 하나씩 눌러 확인했습니다.
평소 쓰시는 크롬 칸과 BARODU Tools 칸은 건드리지 않았고, 끝나고 곱게 닫았습니다.

| 확인한 것 | 결과 |
|---|---|
| 첫 화면 | 정상 · 빈 화면 없음 · 멈춤 없음 |
| 개발용 문구 노출 | 없음 (개발 주소·임시 주소·영어 오류말 모두 없음) |
| 자동입력 | 정상 |
| 직접입력 | 정상 · 작업 도구 7개 |
| 탭 오가도 값 유지 | 정상 |
| 자동저장 | 새로고침해도 이어짐 |
| PC 미리보기 | 가로 스크롤 없음 · 잘린 곳 0곳 |
| 모바일 미리보기 | 가로 스크롤 없음 · 잘린 곳 0곳 |
| 사진 | 정상 (올리는 자리 · 여러 장 한 번에 · 원본 그대로 안내) |
| 구성 | 정상 (메뉴 추가 · 편집 · 복제 · 숨기기/보이기 · 삭제 · 끌어서 순서 바꾸기) |
| 디자인 | 정상 (간편 스타일 6가지 · 글꼴 · 색 · 크기 · 여백) |
| 글자 툴바 (상품정보 안) | 정상 (제목 · 소제목 · 본문 · 직접 · 굵게 · 왼쪽/가운데/오른쪽 정렬) |
| 글자 툴바 → 미리보기 반영 | 정상 (소제목 34→27px, 정렬·굵게 바로 반영) |
| PROMPTER | 정상 (기본형 · ChatGPT용 · Claude용 · 안내문 그대로 있음) |
| 내 작업 | 정상 (이름 붙여 보관 · 작업파일 주고받기) |
| 완성·저장 | 정상 · 네 가지 단추 모두 누를 수 있음 |
| 분할 JPG | 정상 (13초 · 4장 · 이름 순서 정상) |
| 전체 JPG | 정상 (1720×7550) |
| ZIP | 정상 (분할 4장 들어 있음) |
| 작업파일 (.saypage) | 정상 |
| 스마트플레이스 15항목 | 정상 · 항목명 중복 없음 |
| 개별 복사 | **정상 — 붙여넣기까지 확인했습니다** |
| 전체 내용 복사 | 정상 (553자) |
| 메모장 파일 (TXT) | 정상 (한글 안 깨짐 · 15항목 · 순서 정상 · 중복 없음 · 개발자 정보 없음) |
| 스마트스토어 영역 | 정상 표시 · 보류 안내 있음 · "마지막 등록 단추는 누르지 않습니다" 있음 |
| 준비 중 기능 4가지 | 완성된 기능처럼 보이지 않음 (주소를 넣으면 준비 중이라고 안내) |

개별 복사는 **실제로 붙여넣어** 값만 복사되는지 확인했습니다.

| 항목 | 복사된 것 |
|---|---|
| 업체명 | `오늘사진관` |
| 전화번호 | `02-000-0000` |
| 주소 | `경기도 광명시 오리로 000` |
| 영업시간 | `10:00~19:00` |

번호도, 항목 이름도 따라붙지 않았습니다.

## 링크로 가져오기 — 기능은 멀쩡합니다

설치된 제작기에서 **실제 주소 3개**로 돌려봤습니다. 셋 다 잘 들어옵니다.

| 주소 | 가져온 것 |
|---|---|
| 네이버 블로그 (수피아스튜디오) | 상호 · 지역 · 주소 · 전화 · 예약링크 · 촬영상품 · 가격 · 콘셉트 + **사진 6장** |
| 일반 홈페이지 (neeip.com) | 상호 · 지역 · 주소 · 전화 · 휴무일 · 촬영상품 + **사진 5장** |
| 네이버 스마트플레이스 (조아패밀리 광명점) | 상호 · 지역 · 주소 · 전화 · 영업시간 · 휴무일 · 예약링크 · 촬영상품 · 가격 · 콘셉트 + **사진 10장** |

**다만 공개 주소에서는 쓸 수 없습니다.**
BARODU Tools 는 이 컴퓨터에 설치된 제작기에게만 답하도록 만들어져 있습니다.
실제로 불러보니 `막힘` 이었습니다. 화면에서도 **"이 주소에서는 쓸 수 없습니다"** 라고 그대로 알립니다.

서포터즈는 BARODU Tools 가 설치되어 있지 않으므로 어차피 쓰지 못합니다.
**배포 때문에 깨진 것이 아니라, 원래 그렇게 만들어 둔 경계선입니다.**
이걸 열려면 BARODU Tools 쪽을 고쳐 새 설치파일을 만들어야 하고,
인터넷에 올라간 화면이 이 컴퓨터의 프로그램에 말을 걸 수 있게 되는 것이라 권하지 않습니다.
→ **사장님 결정이 필요한 항목입니다.**

## 2차 개선 후보 (이번에 고치지 않음)

1. **분할 JPG 에서 글줄 하나가 두 장에 걸쳐 반씩 잘립니다.** 내용은 안 없어지고 이어집니다.
2. **분할 JPG 맨 끝에 빈 장이 한 장 더 생깁니다.** 전체 JPG 에도 흰 꼬리가 남습니다.
   → 1·2 는 원인이 같습니다. 그림 높이를 실제 내용보다 길게 잡습니다.
      `src/services/export/exportImage.ts` 의 높이 잡는 곳과 `pickCuts` 를 손보면 됩니다.
3. 공개 주소에서 링크로 가져오기를 쓰게 할지 (위 참고 · 사장님 결정)

## 지금 상태

| 무엇 | 값 |
|---|---|
| GitHub | `gamdongstudio/page-maker` · `main` · 새 저장소 만들지 않음 |
| 1차 표시 | `v1-supporters` |
| Cloudflare Pages | `barodu-page-maker` · 새 프로젝트 만들지 않음 |
| 공개 주소 | https://barodu-page-maker.pages.dev |
| 만들기 | `npm run build` · 오류 0 · 결과 `dist` |
| 이번에 고친 코드 | **없음** |

## 판정

**BARODU PAGE MAKER 1차 공개본 최종 검수 통과 — 서포터즈 배포 가능**

## 다음 단계

서포터즈 실제 사용 테스트. 쓰다가 막히는 곳을 모아주세요.
2차에서는 위 **분할 JPG 흠 두 가지**부터 고치는 것을 권합니다.

---

# 1차 공개본 (2026-09-16) — 서포터즈에게 보낼 주소

## 보낼 주소

**https://barodu-page-maker.pages.dev**

| 무엇 | 값 |
|---|---|
| 소스 보관 | https://github.com/gamdongstudio/page-maker |
| 기준 가지(branch) | `main` (016d78d 이후) |
| 1차 표시(tag) | `v1-supporters` |
| Cloudflare 프로젝트 | `barodu-page-maker` |
| Cloudflare 계정 | suminlyu@gmail.com (사장님 계정) |
| 만드는 명령 | `npm run build` (= `tsc -b && vite build`) |
| 결과 폴더 | `dist` |
| 만들기 결과 | 오류 0 · 107개 조각 · 462KB |

## 공개 주소에서 실제로 해본 결과

사장님 컴퓨터의 크롬을 **임시 칸**으로 잠깐 열어 실제로 눌러보며 확인했습니다.
(평소 쓰시는 크롬 칸과 BARODU Tools 칸은 건드리지 않았고, 끝나고 곱게 닫았습니다)

| 확인한 것 | 결과 |
|---|---|
| 첫 화면 | 정상 |
| 자동입력 (붙여넣기 읽기) | 정상 — 값 13개 모두 맞음 |
| 직접입력 · 작업 도구 7개 | 상품정보 · 사진 · 구성 · 디자인 · PROMPTER · 내 작업 · 완성 모두 열림 |
| 미리보기에서 바로 고치기 | 정상 |
| 실행취소 / 다시하기 | 정상 (Ctrl+Z / Ctrl+Shift+Z) |
| 자동저장 | 새로고침해도 이어짐 |
| PC / 모바일 | 860 / 390 으로 바뀜 |
| **분할 JPG** | **정상 저장됨** — 13초, 4장, 이름 순서 정상 (아래 '흠' 참고) |
| 전체 JPG | 정상 (1720×7550) |
| ZIP | 정상 (분할 4장 들어 있음) |
| 작업파일 (.saypage) | 정상 |
| 스마트플레이스 등록자료 | 15개 항목 정상 |
| 항목별 복사 · 전체 내용 복사 | 정상 |
| 메모장 파일 (TXT) | 정상 |
| 개발용어 노출 | 없음 |

## 분할 JPG — 흠 두 가지 (고치지 않고 남겨둠)

이번은 배포·검수만 하는 차례라 **코드를 건드리지 않았습니다.**

1. **글줄 하나가 두 장에 걸쳐 반씩 잘립니다.**
   1장 맨 아래 `오래 걸어둘 수 있는 보정과 액자` 의 윗반, 2장 맨 위에 아랫반.
   내용이 없어지지는 않지만 화면 안내("글자 한가운데가 잘리지 않습니다")와 다릅니다.
2. **끝에 흰 여백이 크게 남고, 빈 장이 한 장 더 생깁니다.**
   3장 아래쪽 절반이 비어 있고 4장은 전부 흰색입니다.

원인은 같습니다. 그림을 그릴 때 잡는 높이(`scrollHeight`)가 실제 내용보다 길어서,
남는 빈 자리까지 그려지고 자를 자리가 여백이 아닌 곳으로 밀립니다.
고치려면 `src/services/export/exportImage.ts` 의 높이 잡는 부분과
`pickCuts` 의 '여백까지 너무 멀면 중간에서 자른다' 부분을 손봐야 합니다.

## BARODU Tools 연결 — 공개 주소에서는 안 됩니다

실제로 불러봤습니다. `127.0.0.1:5199` → **막힘 (Failed to fetch)**.
BARODU Tools 는 이 컴퓨터에 설치된 제작기에게만 답하도록 만들어져 있습니다.

| 기능 | 공개 주소 |
|---|---|
| 링크로 가져오기 (블로그 · 스마트플레이스 · 홈페이지) | 안 됨 |
| 스마트스토어 자동입력 | 안 됨 (지금 보류 상태 그대로) |

화면에서 **"이 주소에서는 쓸 수 없습니다"** 라고 그대로 알립니다.
"설치하면 된다" 고 말하지 않습니다.
Tools 가 필요 없는 기능(붙여넣기·사진·구성·디자인·저장·등록자료)은 모두 그대로 됩니다.

주소 종류는 Tools 없이도 알아봅니다 — 넣어보면 이렇게 안내합니다.

| 넣은 주소 | 안내 |
|---|---|
| 네이버 블로그 | 글과 사진을 잘 가져옵니다 |
| 네이버 스마트플레이스 | 업체정보를 가져옵니다 · 화면이 자주 바뀌어 못 읽을 때도 있습니다 |
| 일반 홈페이지 | 글과 사진을 가져옵니다 |

## 준비 중 (이번에 만들지 않음 · 코드는 남겨둠)

| 무엇 | 화면 안내 |
|---|---|
| 스마트스토어 기존자료 가져오기 | "스마트스토어 전용으로 읽는 방법은 아직 준비 중입니다" |
| 인스타그램 가져오기 | "로그인해야 볼 수 있는 글이 많아 지금은 대부분 읽지 못합니다" |
| 여러 URL 통합 | 미구현 |
| 유사사진 자동 그룹화 | 미구현 |

## 알려진 제한

- 공개 주소에서는 링크로 가져오기·스마트스토어 자동입력이 안 됩니다 (위 참고)
- 분할 JPG 에 흠 두 가지 (위 참고)
- 지금 배포 방식은 **직접 올리기**입니다. GitHub 와 이어 붙이려면
  Cloudflare 화면에서 한 번 눌러 승인해 주셔야 합니다 (아래)

## GitHub 와 이어 붙이기 (사장님이 한 번만 눌러주시면 됩니다)

지금은 `npm run build` 뒤 아래 한 줄로 올립니다.

```
npx wrangler pages deploy dist --project-name=barodu-page-maker --branch=main
```

`main` 에 올리면 저절로 다시 배포되게 하려면
Cloudflare 화면 → **Workers & Pages → barodu-page-maker → Settings → Builds**
→ **Connect to Git** → `gamdongstudio/page-maker` 고르기
→ 만드는 명령 `npm run build` / 결과 폴더 `dist` / 기준 가지 `main`.

이건 GitHub 승인 창이 떠서 **사장님이 직접 눌러주셔야** 합니다.
지금 방식으로도 공개 주소는 그대로 살아 있습니다.

## 다음 단계

**서포터즈 실제 사용 테스트.**
위 주소를 보내시고, 쓰다가 막히는 곳을 모아주세요.
분할 JPG 흠 두 가지는 그 다음에 고치는 것을 권합니다.

---

# 공개 주소 (2026-09-16) — 누구나 열어볼 수 있는 주소

## 주소

| 무엇 | 주소 |
|---|---|
| **공개 주소 (안 바뀜)** | https://barodu-page-maker.pages.dev |
| 소스 보관 | https://github.com/gamdongstudio/page-maker |

## 계정 · 저장 위치

| 무엇 | 값 |
|---|---|
| GitHub 계정 | `gamdongstudio` (사장님 계정 · 새로 만들지 않음) |
| 저장소 | `gamdongstudio/page-maker` |
| 가지(branch) | `main` |
| 표시(tag) | `v1-supporters` — 서포터즈 테스트용 1차본 |
| Cloudflare 계정 | suminlyu@gmail.com (사장님 계정 · 새로 만들지 않음) |
| Cloudflare 프로젝트 | `barodu-page-maker` |

## 올리지 않은 것 (확인함)

네이버 아이디·비밀번호, 로그인 기록, 쿠키, 크롬 사용자 칸,
BARODU Tools 크롬 칸, 비밀키·토큰, `node_modules`, `dist`, 임시 파일.
막아둔 목록은 `.gitignore` 에 있습니다.

## 새로 고친 뒤 공개 주소에 반영하는 법

```
npm run build
npx wrangler pages deploy dist --project-name=barodu-page-maker --branch=main
```

소스도 같이 남기려면 `git push origin main` 을 함께 하세요.

## 공개 주소에서 **되는 것** (실제로 확인함)

- 화면 열림 · 자동저장 (새로고침해도 이어짐)
- 내용 붙여넣기로 읽기 — 16항목 확인 화면, 값 13개 모두 정확
- AI로 상세페이지 만들기 (확인 화면 → 적용)
- 미리보기에서 글자 바로 고치기, 실행취소
- 저장용 화면에 편집 표시 **0개** (저장 이미지에 편집 단추가 섞이지 않음)
- 네이버 스마트플레이스 등록자료 15항목 · 메모장 파일 받기
- 네이버 스마트스토어 준비 상태 · 항목별 복사
- 작업파일(.saypage) 저장
- 휴대폰 폭(375)에서 가로로 밀리지 않음

## 공개 주소에서 **안 되는 것** (그대로 알립니다)

| 무엇 | 왜 |
|---|---|
| 링크로 가져오기 | BARODU Tools 는 **이 컴퓨터에 설치된 제작기**에게만 답합니다. 인터넷 주소로 열면 설치해도 이어지지 않습니다. |
| 스마트스토어 자동입력 | 같은 이유입니다. |

두 기능은 화면에서 **"이 주소에서는 쓸 수 없습니다"** 라고 그대로 안내합니다.
"설치하면 된다" 고 말하지 않습니다. 필요하시면 컴퓨터에 설치한 제작기에서 쓰세요.

## 아직 확인하지 못한 것

**이미지로 저장(분할 JPG · 전체 JPG · ZIP)** 은 공개 주소에서 확인하지 못했습니다.
제가 쓰는 확인용 창이 화면에 보이지 않는 창이라 그림을 그리지 못합니다.
프로그램이 잘못된 것이 아니라 **확인 방법의 한계**입니다.
다만 오래 걸릴 때 `이미지를 만드는 시간이 너무 오래 걸립니다` 라고 알리고
**작업 내용은 그대로 남는 것**은 확인했습니다.

→ 사장님 크롬에서 https://barodu-page-maker.pages.dev 를 열고
   **완성·저장 → 분할 JPG로 저장** 을 한 번만 눌러봐 주세요.

---

# 이번 세션 기록 (2026-09-16, 화면 구조 정리)

## 이번 세션에서 수정한 파일

| 파일 | 한 일 |
|---|---|
| `src/components/ui/Icon.tsx` | **새로 만듦** — 선 아이콘 한 벌 (인라인 SVG). 바깥 라이브러리 설치 안 함 |
| `src/components/editor/EditorPanel.tsx` | 1층(자동입력·직접입력) / 2층(작업 도구) 구조, 이모지 → 아이콘, 배지 단순화 |
| `src/App.tsx` | 시작 화면 없이 바로 진입, 상단 아이콘 단추, 쓰지 않게 된 `openAi` 제거 |
| `src/components/ai/AiPanel.tsx` | 링크 가져오기를 **맨 위(STEP 1)** 로, 범위 선택을 카드형으로, STEP 번호 정리 |
| `src/services/ai/applyPlan.ts` | `SCOPE_LABEL` 에 카드용 짧은 말(`short`·`hint`) 추가 (기능 변화 없음) |
| `src/styles/app.css` | `.modetabs` `.toolnav` `.scopegrid` `.scopecard` `.btn--icon` 등 추가 |

> `src/components/editor/StartHere.tsx` 는 **지우지 않고 렌더만 중단**했습니다.
> 되돌리기 쉽게 남겨둔 것입니다. 지금 화면에는 나오지 않습니다.

## 완료한 UI 항목 (실제 화면에서 확인함)

- 큰 시작 카드(`어떻게 시작할까요?`) 제거 — 열면 바로 제작 화면
- `AI 제작` → **자동입력**
- **자동입력 / 직접입력 1층 탭** 추가
- 탭을 오가도 **데이터 그대로 유지** (상호·사진 3장 왕복 확인)
- 2층 작업 도구 7개 분리 (상품정보·사진·구성·디자인·PROMPTER·내 작업·완성)
- 컬러 이모지 **0개**
- 선 아이콘 통일 (크기·굵기 같음, 고른 것만 파란색)
- 상태 배지 단순화 — 작은 체크 / 작은 점만
- 진행률 % 없음 → **"완성 전 확인할 항목 N개"**
- 링크 가져오기를 자동입력 **맨 위**로
- 만들 범위를 **3열 카드 6개**(추천 표시 포함)로
- 모바일 375px — 가로 스크롤 없음, 범위 카드 2열

**그대로 둔 것(이미 잘 되고 있어 건드리지 않음)**:
폰트 5종(글꼴 미리보기 문장 포함) · PROMPTER 3종(기본형/ChatGPT/Claude, Codex 없음) ·
링크 가져오기 기능 · 자동저장 · 실행취소/다시하기 · 내 작업 · 출력(JPG·분할·ZIP·.saypage)

## build 상태

**빌드 끝났습니다.** `npm run build` 성공, 오류 0.
`dist/assets/index-BZ563Tw_.js` · `index-DbO9mdeL.css`

## localhost:5180 반영 확인

**확인했습니다.** 위 '완료한 UI 항목' 은 전부 실제 화면에서 본 것입니다.
읽는 규칙 회귀 시험도 **70/70 통과** (`npm run test:parse`).

> 다만 화면 서버(`BARODU PAGE MAKER 시작.cmd`)를 껐다 켜면
> 그때 dist 를 다시 읽으므로, 다음에 열어도 이 내용 그대로입니다.

## 남은 작업 (다음 세션)

우선순위 순입니다.

1. **사진 업로드 영역 강화** — 지금은 안내문이 길어 부가기능처럼 보임
2. **직접입력 아코디언** — 기본정보 / 가격·구성 / 제공항목 / 사진관 정보로 접기.
   지금은 입력칸이 한 번에 다 펼쳐짐
3. **글 입력 근처 폰트 도구막대** — 폰트가 디자인 단계에만 있어 글 쓰는 자리에서 멀다.
   `StepDesign` 의 기존 폰트 컴포넌트를 **재사용**할 것 (새로 만들지 말 것)
4. **디자인 스타일 미니 미리보기 카드** — 지금은 색·글꼴 견본 수준
5. 미리보기 hover 수정 아이콘 / 부분 자동 다듬기(준비 중 표시) /
   내 작업 카드형 / 완성·저장을 목적 중심 문구로

## ✅ 2차 UI 작업 완료 (2026-09-16) — 아래 7가지 전부 반영됨

| 순서 | 항목 | 결과 |
|---|---|---|
| 1 | 사진 업로드 영역 강화 | 큰 영역 + 아이콘 + `사진 추가` 단추. 긴 약속문은 **접어둠**(`foldnote`) |
| 2 | 사진관리 아이콘 정리 | 대표로·위치 조정·삭제에 선 아이콘 |
| 3 | 글 입력 근처 폰트 툴바 | **새 기능 아님** — ④ 디자인의 글꼴 값을 그대로 고침. 상품정보 맨 위에 붙임 |
| 4 | 디자인 스타일 미니 카드 | 사진·제목·**가격(250,000→189,000원)**·버튼이 보이는 미리보기 |
| 5 | 미리보기 hover 수정 | 올렸을 때만 작은 `수정` 표시. **저장 이미지에는 안 들어감** |
| 6 | 내 작업 카드 | 이미 `workcard` 카드형이라 **그대로 둠** |
| 7 | 완성·저장 문구 | 목적 중심 4개 (스마트스토어 / 한 장 / 백업·전달 / 다시 편집) |

**수정한 파일**: `AiPanel.tsx` · `StepMedia.tsx` · `StepProduct.tsx` · `StepDesign.tsx` ·
`DetailPage.tsx` · `StepCheck.tsx` · `styles/app.css`
**새로 만든 파일**: `src/components/design/TextToolbar.tsx`

**build**: 성공 (`index-AMMxfZfx.js` · `index-DgAYMmgY.css`)
**localhost:5180 확인**: 완료 — 폰트 바꾸면 미리보기 즉시 반영(Pretendard→GmarketSans),
크기 조절, 디자인 카드 6개, 저장 4종 문구 확인

**회귀 확인**: 링크 가져오기(10항목·10장) · 사진 적용 · 자동저장 · Tools 1.0.5 연결 — 이상 없음

### 남은 것

- 직접입력 아코디언 (기본정보 / 가격·구성 / 제공항목 / 사진관 정보로 접기) — **아직 안 함**
- 부분 자동 다듬기(`준비 중` 표시) — 아직 안 함

---

## ▶ 지난 세션 시작 지시 (완료됨, 참고용)

**이 파일을 먼저 읽고 시작하세요.**

- 위 '완료한 UI 항목' 은 **다시 만들지 마세요.**
- **build 와 localhost:5180 반영 확인은 이미 끝났습니다. 처음에 반복하지 마세요.**
- 아래 순서대로 **남은 작업 1번부터** 바로 이어서 진행하세요.

### 진행 순서

1. 사진 업로드 영역 강화
2. 사진관리 아이콘 정리
3. 글 입력 근처에 **기존** 폰트 툴바 연결
4. 디자인 스타일 미니 미리보기 카드
5. 미리보기 hover 수정
6. 내 작업 카드
7. 완성·저장 문구 정리

### 지킬 것

- 이미 있는 기능은 **새로 만들지 말고 기존 컴포넌트를 재사용**
  (폰트 → `StepDesign`, 사진 → `StepMedia`, 내 작업 → `StepWorks`, 아이콘 → `components/ui/Icon.tsx`)
- **BARODU Tools 는 건드리지 마세요** (Playwright · parser · image proxy · Setup.exe · Tools API 포함)
- 작업 중간 설명은 최소화하고, 실제 수정과 테스트에 토큰을 쓰세요
- **모든 UI 수정이 끝난 뒤 마지막에 한 번만** `npm run build` →
  `http://localhost:5180` 에서 실제 화면 확인

> 주의: `localhost:5180` 은 **소스가 아니라 `dist`** 를 내보냅니다.
> 그래서 마지막 build 를 빠뜨리면 화면에 반영되지 않습니다.

---

## 마무리 작업 (2026-09-16 밤 — 직접입력 접기 · 준비 중 표시)

### 수정한 파일

| 파일 | 한 일 |
|---|---|
| `src/components/studio/StudioForm.tsx` | **새로 만듦** — `AiPanel.tsx` 안에 있던 사진관 정보 입력칸을 **그대로 옮겨** 두 곳에서 같이 쓰게 함 (기능·저장 방식 변화 없음) |
| `src/components/ai/AiPanel.tsx` | 위 입력칸 정의를 빼고 **불러다 쓰기**로 바꿈 (화면 변화 없음) |
| `src/components/editor/StepProduct.tsx` | 직접입력을 **4개 접이식 묶음**으로 정리 + `부분 자동 다듬기` **준비 중 표시** 추가 |
| `src/styles/app.css` | `.foldbox*` (접이식 묶음), `.soonchip` (준비 중 표시) 추가 |

> 입력칸은 **하나도 지우지 않았습니다.** 자리만 옮겼습니다.
> 저장하는 값·자동저장·실행취소도 예전 그대로입니다. 새 데이터 구조를 만들지 않았습니다.

### 직접입력 접기 — 완료

| 묶음 | 안에 들어간 것 | 처음 상태 |
|---|---|---|
| 기본정보 | 상품명 · 촬영 종류 · 사진관명 · 한 줄 소개 · 정상가 · 판매가 · 예약·문의 | **열림** |
| 가격·구성 | 촬영 구성·제공 항목 · 촬영 안내 · 예약 링크 | 접힘 |
| 제공항목 | 의상·헤어·메이크업 · 추천 대상 · 상세 설명 · 유의사항 · 기타 설명 | 접힘 |
| 사진관 정보 | 사진관 공통정보 입력칸 (자동입력에서 쓰던 것과 같은 것) | 접힘 |

### `http://localhost:5180` 에서 실제로 확인한 것

- 4개 묶음 이름이 모두 보임, **처음에는 기본정보만 열림**
- 접힌 묶음은 입력칸이 **화면에 아예 없음** (눌러야 나옴) — 눌러서 나오는 것 확인
- 묶음을 열어도 다른 묶음은 닫히지 않음
- 상품명에 글을 넣으면 **미리보기에 바로** 나옴
- 자동입력 ↔ 직접입력 을 오가도 **적은 글 그대로 남음**
- 글씨 크기 단추 → 미리보기 글씨가 실제로 커짐 (41px → 43px)
- `부분 자동 다듬기` 는 **눌리지 않고** `준비 중 · AI 연결 후 사용 가능` 이라고만 나옴

### build

`npm run build` **성공** (타입 오류 없음).

### 저장(출력) 다시 확인 — 절반만 됨

- **작업파일(.saypage) 저장** — 눌렀을 때 파일이 실제로 만들어짐 ✔
- **전체 JPG · 분할 JPG · ZIP** — **이 자동 시험용 브라우저에서는 확인하지 못했습니다.**
  `이미지를 만드는 시간이 너무 오래 걸립니다` 로 끝났습니다.
  창이 뒤에 가려져 화면이 그려지지 않으면 이미지 만들기가 멈추는, **전부터 있던 시험 환경 문제**입니다.
  (사진 때문이 아닙니다 — 사진 0장으로도 같았습니다.)
  이번에 **출력 관련 코드는 한 줄도 건드리지 않았고**,
  같은 코드로 어제 저장한 `새_상세페이지_전체.jpg` · `새_상세페이지_01.jpg` · `새_상세페이지.zip` 이
  `다운로드` 폴더에 그대로 있습니다.
  → **사장님이 직접 한 번 눌러서 확인해 주세요.** 이것이 지금 남은 유일한 미확인 항목입니다.

- 이미지 만들기가 실패해도 **적어둔 내용은 그대로 남아 있었습니다** (초기화되지 않음) ✔

---


---

## 미리보기 직접 편집 (2026-09-16 — 왼쪽에서 바로 고치기)

### 어느 프로젝트를 고쳤나 (작업 전 확인함)

| 무엇 | 어디 |
|---|---|
| `localhost:5180` 을 띄우는 것 | `SAY-PAGE-MAKER-PRO\scripts\serve.mjs` (node, PID 확인) |
| 내보내는 화면 파일 | `SAY-PAGE-MAKER-PRO\dist` (`index-…js` 이름이 이번 build 와 일치함) |
| 고친 소스 | `SAY-PAGE-MAKER-PRO\src` |

> `SAY-PAGE-MAKER-PRO-26` 은 **9월 14~15일의 옛 복사본**입니다. `dist` 도 `scripts` 도 없습니다.
> 화면과 아무 상관이 없으므로 **손대지 않았습니다.**
> `PAGE-MAKER` · `PAGE-MAKER-SMARTPLACE` · `PAGE-MAKER-SMARTSTORE` · `_backup-before-barodu` 도 그대로입니다.

### 새로 만든 파일

| 파일 | 하는 일 |
|---|---|
| `src/components/preview/Editable.tsx` | 미리보기에서 **글자를 눌러 바로 고치는** 칸 |
| `src/components/preview/editApi.ts` | 미리보기 ↔ 실제 값 변경을 잇는 고리 (한 곳에 모아둠) |
| `src/components/menus/AddMenuHere.tsx` | 미리보기의 `+ 여기에 넣기` 고르기 창 |

### 고친 파일

| 파일 | 한 일 |
|---|---|
| `src/components/preview/DetailPage.tsx` | 섹션 도구(위/아래/복제/숨기기/삭제) · 섹션 사이 `+` · 사진 hover 도구 · 글자 직접 수정 |
| `src/components/preview/sections.tsx` | 이벤트 포스터형 · 가격표/상품 비교 · 자유 영역 |
| `src/components/preview/templates.ts` | 이벤트 `포스터형`, 가격 `가격표형`, 상품 비교 2가지 추가 |
| `src/components/preview/Preview.tsx` | 편집 고리 전달 |
| `src/App.tsx` | 실제 값 변경·확인창·요소별 오른쪽 이동 |
| `src/components/menus/SectionTools.tsx` | 촬영상품(가격표) 편집 · 이 섹션 글자만 · 자유 영역 버튼 문구 |
| `src/components/menus/StepMenus.tsx` | 가격 여러 개 발견 시 제안 · `말로 구성 바꾸기` 준비 중 표시 |
| `src/components/media/StepMedia.tsx` | 사진을 **카드 4장씩** · 크게 보기 · 카드에서 바로 삭제 |
| `src/components/design/TextToolbar.tsx` | 제목/소제목/본문 크기 · 직접 · 굵게 · 정렬 3가지 |
| `src/types/project.ts` | `free` 영역, 섹션 글꼴, 버튼 문구, 촬영상품 목록, 오른쪽 정렬, 굵게 (**모두 선택 항목** — 옛 작업파일 그대로 열림) |
| `src/types/defaults.ts` | 새 상세페이지 기본 구성 11개 · 중요 영역 표시 · 메뉴 이름 정리 |
| `src/styles/app.css` | 위 화면들의 모양 |

### 실제 화면에서 확인한 것 (`http://localhost:5180`)

| 항목 | 결과 |
|---|---|
| 미리보기 글자 직접 수정 (상품명·사진관명·한 줄 소개·섹션 제목·본문·가격) | 됨 — 고치면 곧바로 반영, 자동저장, 실행취소도 됨 |
| 가격은 `150,000원` 으로 보이고 고칠 때는 숫자만 | 됨 |
| 섹션 위/아래/복제/숨기기/삭제 | 됨 |
| 섹션 사이 `+ 여기에 넣기` → 메뉴 27종 고르기 | 됨 |
| 미리보기 섹션을 누르면 오른쪽 `구성`의 그 섹션이 열림 | 됨 |
| 새 상세페이지 기본 구성 11개 (가격 안내·이벤트·혜택·예약·문의 포함) | 됨 |
| 중요 영역 숨기기·삭제 전 확인 | 됨 (취소하면 그대로 남음) |
| 가격표 · 상품 비교 (상품 추가/복제/삭제/순서) | 됨 — 미리보기에서 상품명·가격 바로 수정됨 |
| 이벤트 포스터형 | 됨 |
| 자유 영역 (제목·본문·버튼) | 됨 |
| 사진 카드 — 넓은 화면 4열 / 모바일 2열 | 됨 |
| 글씨 제목/소제목/본문 · 직접 · 굵게 · 왼쪽/가운데/오른쪽 | 됨 |
| 모바일 375px — 가로 스크롤 없음 | 됨 |
| **편집용 표시가 저장 이미지에 들어가는지** | **하나도 없음** (저장용 화면에서 0개 확인) |

### 아직 못 한 것 · 확인하지 못한 것

- **미리보기에서 사진 순서를 끌어서 바꾸기** — 넣기·교체·대표·삭제는 되지만 순서 바꾸기는 `사진` 화면에서만 됩니다.
- **사진 넣기·교체를 실제 파일로 눌러보는 것** — 자동 시험 도구로는 파일 고르기 창을 띄울 수 없어 확인하지 못했습니다. (사진 올리기 자체는 `사진` 화면에서 5장 올려 확인했습니다.)
- **전체 JPG · 분할 JPG · ZIP 저장** — 자동 시험용 브라우저에서는 `시간이 너무 오래 걸립니다` 로 끝납니다. 창이 가려지면 화면이 그려지지 않아 멈추는, 전부터 있던 시험 환경 문제입니다. **사장님이 직접 한 번 눌러 확인해 주세요.**
- **자동입력 STEP 7단계 재배치** — 손대지 않았습니다. 지금 순서 그대로입니다.
- **말로 구성 바꾸기** — `준비 중 · AI 연결 후 사용 가능` 표시만 있습니다. 동작하지 않습니다.

### 시험하며 생긴 일

시험 중 화면에 임시 글자·사진을 넣었다가 **`새로 시작`으로 되돌렸습니다.**
그 안에 들어 있던 것은 지난 시험용 `새 상세페이지` 한 개뿐이었고, 사진관 공통정보(상호·전화 등)는 따로 보관되어 그대로 있습니다.

---

## 스마트스토어 등록 지원 (2026-09-16 밤 — 여기서 중단)

### 오늘 완료한 작업

| 무엇 | 상태 |
|---|---|
| 스마트스토어에 올릴 내용 한 벌(`SmartStorePayload`) 만들기 | **완료** |
| 완성·저장 안에 `네이버 스마트스토어` 영역 | **완료** |
| 등록 전 준비 상태 표시 | **완료** |
| 등록자료 ZIP (01~04 폴더) | **완료** |
| 스마트스토어_입력내용.txt | **완료** |
| 항목별 복사 · 전체 문구 복사 | **완료** |
| BARODU Tools `publishers/naverSmartStore` 구조 | **완료 (단, 실제 스마트스토어 화면에서는 미검증)** |
| 상품등록 화면 열기 · 로그인 확인 | **완료 (창 열림·중단 지점까지 확인)** |
| 상품명/판매가/설명/이벤트 넣기 · 사진 올리기 | **코드만 있음. 실제로 들어가는지 확인 못 함** |
| 최종 등록 단추 누르지 않기 | **완료 (실제로 누르지 않는 것 확인)** |

> 곁다리로 하나 고쳤습니다: `직접입력 → 사진관 정보` 에 적은 값이
> **지금 작업에도 저장되지 않던 문제**를 고쳤습니다.
> 이제 적으면 미리보기의 예약·문의 버튼과 스마트스토어 자료에 바로 반영됩니다.

### SmartStorePayload / TXT / ZIP 생성 상태

**모두 한 곳에서 나옵니다.** 자동입력·ZIP·TXT·복사 단추가 같은 값을 씁니다.

- `src/services/smartstore/payload.ts` — **여기가 기준입니다.**
- `src/services/smartstore/text.ts` — 복사용 문구 · 전체 TXT
- `src/services/smartstore/pack.ts` — 파일 이름 · ZIP
- `src/services/smartstore/publish.ts` — BARODU Tools 로 넘기는 쪽

실제로 만들어 본 ZIP 안 (확인함):

```
01_대표사진/광명가족사진_감동사진관_대표.jpg
02_추가사진/광명가족사진_감동사진관_01.jpg  (02, 03 …)
03_상세페이지/…
04_입력문구/스마트스토어_입력내용.txt
```

### PAGE MAKER 스마트스토어 화면 상태

`완성·저장` 을 누르면 맨 위에 `네이버 스마트스토어` 영역이 나옵니다.

- 준비 상태 9줄 (상품명·판매가·설명·대표사진·추가사진·상세페이지·이벤트 + 카테고리·배송은 `확인 필요`)
- `[스마트스토어 자동입력]` — BARODU Tools 가 필요합니다
- `[스마트스토어 등록자료 받기]` — **Tools 없이도 됩니다**
- `항목별로 복사하기` — 항목마다 [복사], 맨 아래 [전체 문구 복사]
- 올릴 사진 이름 미리보기

### BARODU Tools publisher 구현 상태

새로 만든 파일 (기존 collector·image proxy 는 **건드리지 않았습니다**):

```
publishers/naverSmartStore/index.js       전체 흐름
publishers/naverSmartStore/selectors.js   어느 칸이 어디 있는지 ← 화면 바뀌면 여기만
publishers/naverSmartStore/session.js     창 열기 · 로그인 상태 보관
publishers/naverSmartStore/paste.js       메모장 → 클립보드 → 직접입력 순서
server/routes/publish.js                  POST /v1/publish/smartstore
server/index.js                           위 길 연결 + 보낼 수 있는 크기 1mb → 80mb
```

- 아이디·비밀번호는 **어디에도 저장하지 않습니다.** 로그인은 사장님이 직접 하십니다.
- 카테고리·배송·반품·출고지는 **손대지 않습니다.**
- 마지막 등록·저장·판매시작 단추는 **누르지 않습니다.** (`selectors.js` 의 `neverClick` 은 누르지 않기 위한 목록입니다)

### 실제로 테스트한 것

- 준비 상태 9줄이 실제 값대로 뜨는 것
- 등록자료 ZIP 만들기 → 폴더 4개 · 사진 이름 · TXT 들어 있는 것
- 화면의 복사 항목과 ZIP 안 TXT 내용이 같은 것
- 원본 사진은 그대로, 복사본에만 이름이 붙는 것
- 이름에 같은 검색어가 반복되지 않는 것
- BARODU Tools 없이도 등록자료 받기가 되는 것 (설치본 1.0.5 는 자동입력을 모르지만 ZIP 은 나옴)
- 새 코드로 publish 길이 열리고, 창이 뜨고, **등록 단추를 누르지 않고 멈추는 것**
- 설치본 1.0.5 에 자동입력을 요청하면 404 → 화면에 `새 버전으로 올려주세요` 라고 정직하게 나오는 것
- 기존 기능 그대로: 자동입력(링크 가져오기) · 사진 · 구성 · 자동저장 · 실행취소

### 아직 테스트하지 못한 것

- **스마트스토어 상품등록 화면에 실제로 값이 들어가는지** — 판매자 계정 로그인이 필요합니다.
  지금 `selectors.js` 의 입력칸 위치는 **짐작으로 적은 것**이고 맞는지 확인하지 못했습니다.
  → 내일 로그인 후 실제 화면을 보고 고쳐야 합니다.
- 메모장 → Ctrl+A → Ctrl+C → Ctrl+V 가 실제 입력칸에서 되는지
- 대표/추가/상세 사진이 실제로 올라가는지
- 실패 지점부터 다시 시도
- 상세페이지 분할 이미지 — 자동 시험 브라우저에서는 여전히 시간 초과라 ZIP 에 못 담겼습니다.
  (창이 가려지면 화면이 안 그려져 멈추는 전부터 있던 시험 환경 문제 — 사장님 화면에서는 직접 확인해 주세요)

### 현재 설치된 BARODU Tools 버전

**1.0.5** (5199 번에서 실행 중). 이 버전에는 **스마트스토어 자동입력이 들어 있지 않습니다.**

### 새 Setup.exe 빌드가 필요한지

**필요합니다. 다만 아직 하지 마세요.**
입력칸 위치(`selectors.js`)를 실제 화면으로 맞춘 뒤에 한 번만 만드는 것이 낫습니다.
지금 만들면 어차피 다시 만들어야 합니다.

### 오류 또는 보류사항

- 지금 화면에서 `[스마트스토어 자동입력]` 을 누르면 `지금 설치된 BARODU Tools 는 …아직 못 합니다` 가 나옵니다. **정상입니다** (설치본이 1.0.5 이기 때문).
- 스마트스토어 로그인 창을 하나 열어 두었습니다. 로그인해 두시면 내일 그대로 이어집니다.
  **그 창을 닫으시면 됩니다.** 프로그램에는 영향이 없습니다.
  (로그인 상태는 `%LOCALAPPDATA%\BARODU Tools\smartstore-profile` 에만 남고, 아이디·비밀번호는 저장되지 않습니다)
- 상세페이지 이미지를 못 만들면 ZIP 에 `이미지가_없습니다.txt` 가 대신 들어갑니다 (사진·문구는 정상적으로 들어감).

### 내일 가장 먼저 해야 할 작업 1번

**스마트스토어에 판매자 계정으로 로그인한 상태에서 상품등록 화면을 열고,
`BARODU Tools/publishers/naverSmartStore/selectors.js` 의 입력칸 위치를 실제 화면에 맞게 고치기.**

그다음 순서: 상품명 한 칸만 넣어보기 → 나머지 글 → 사진 → 그 뒤에 새 Setup.exe.

---

## 실제 스마트스토어 화면으로 맞추기 (2026-09-16 오전)

어제 남긴 **「내일 1번」** 을 했습니다. 판매자 계정(감동사진관)으로 로그인된 실제
상품등록 화면을 열어 확인했고, **아무것도 등록하지 않았습니다.**

### 실제 화면에서 확인한 것 (눈으로 본 것만)

| 확인 | 결과 |
|---|---|
| 상품등록 화면 주소 | `#/products/create` |
| **주소로 바로 열기** | **안 됩니다.** 새로 열면 대시보드로 튕깁니다 |
| 들어가는 길 | 왼쪽 메뉴 `상품관리` → `상품 등록` (메뉴를 눌러야만 열림) |
| 상품명 칸 | `input[name="product.name"]` — **값이 실제로 들어감** |
| 판매가 칸 | `input[name="product.salePrice"]` — **값이 실제로 들어감** |
| 상세설명 | 스마트에디터(안쪽 화면). `textarea[name="editorContent"]` 는 비었는지만 표시하는 칸이라 **이 길로는 안 됨** |
| 사진 올리는 칸 | 이 화면에 파일 칸(`input[type=file]`)이 **하나도 없음.** 단추를 눌러 파일 고르기 창이 뜨는 방식 |
| 이벤트·혜택 칸 | 기본 화면에 **없음** |
| 정상가(할인) | 할인 설정을 켜야 나오는 칸이라 기본 화면에 없음 |
| 화면 아래 단추 | 미리보기 · 임시저장 · **저장하기** · 취소 → 전부 누르지 않음 |
| 안내창 | 로그인 직후와 상품등록 화면 양쪽에 뜸 → 먼저 닫아야 함 |

> 시험으로 넣었던 상품명·판매가는 **다시 지웠습니다.** 저장은 하지 않았습니다.

### 고친 파일 (BARODU Tools 만)

| 파일 | 한 일 |
|---|---|
| `publishers/naverSmartStore/selectors.js` | 짐작으로 적었던 위치를 **실제 화면 값**으로 교체. 칸마다 `verified` 표시를 두어 확인된 것만 쓰게 함 |
| `publishers/naverSmartStore/index.js` | 주소로 열던 것을 **메뉴를 눌러 들어가는 방식**으로 교체 · 안내창 닫기 추가 · 사진은 파일 고르기 창을 받는 방식으로 교체 · **확인 안 된 칸에는 넣지 않고 알리기** |

PAGE MAKER 쪽은 **한 줄도 고치지 않았습니다.** collector·image proxy 도 그대로입니다.

### 지금 자동으로 되는 것 / 안 되는 것

| 항목 | 지금 상태 |
|---|---|
| 상품등록 화면 열기 | 됨 (메뉴로 들어감) |
| 상품명 | **됨 (확인함)** |
| 판매가 | **됨 (확인함)** |
| 상품 설명 | 안 됨 → `상세설명은 스마트에디터에 직접 붙여넣어 주세요` 라고 알림 |
| 이벤트·혜택 | 칸이 없음 → `상세설명에 함께 넣어주세요` 라고 알림 |
| 정상가 | 안 넣음 → `할인 설정을 켜야 나오는 칸` 이라고 알림 |
| 대표·추가 사진 | 코드는 넣었으나 **아직 확인 못 함** |
| 상세페이지 이미지 | 안 됨 → 직접 올려주세요 |

되는 척하지 않습니다. 못 하는 항목은 화면에 그대로 `직접 넣어주세요` 로 나옵니다.

### 아직 확인하지 못한 것

- 새 코드를 **처음부터 끝까지 한 번에** 돌려보기 (창을 붙잡고 있는 확인용 도구와
  실제 기능이 같은 로그인 창을 함께 쓸 수 없어서 이번에는 못 했습니다)
- 대표·추가 사진이 실제로 올라가는지 (파일 고르기 창 방식)
- 메모장 붙여넣기가 실제 칸에서 되는지 (지금은 직접 입력으로 들어갔습니다)

### 로그인에 대해 알아낸 것 (중요)

**창을 닫으면 로그인이 풀릴 수 있습니다.** 네이버 커머스 ID 가 창을 닫으면 유지되지
않는 경우가 있었습니다. 그래서 자동입력을 쓰실 때는

  창이 열리면 → 로그인 → **끝날 때까지 그 창을 닫지 마세요**

이 안내를 자동입력 실패 문구에도 넣어두었습니다.

### 현재 설치된 BARODU Tools 버전

**1.0.5** — 그대로입니다. 오늘도 새로 만들지 않았습니다.
(자동입력은 아직 설치본에 없습니다. 화면에서 누르면 `새 버전으로 올려주세요` 가 나옵니다 — 정상입니다.)

### 새 Setup.exe 빌드가 필요한지

**아직 이릅니다.** 사진 올리기와 상세설명까지 확인한 뒤에 한 번만 만드는 것이 낫습니다.

### 내일(다음) 가장 먼저 해야 할 작업 1번

**스마트스토어에 로그인한 뒤 자동입력을 처음부터 끝까지 한 번 돌려서
`대표사진 올리기` 가 실제로 되는지 확인하기.**

그다음: 상세설명(스마트에디터) 붙여넣기 길 찾기 → 그 뒤에 새 Setup.exe.

---

## 대표사진 업로드 — **성공** (2026-09-16 낮)

### 결과

**대표사진 1장이 실제로 올라갔고, 화면에 썸네일이 떴습니다.**
올라간 사진 주소가 `shop1.phinf.naver.net/...` 인 것까지 확인했습니다 (네이버 서버에 실제 전송됨).
등록·저장하기·임시저장 단추는 **누르지 않았습니다.**

### 진짜 원인이었던 것

셀렉터 문제가 **아니었습니다.**

> 대표이미지 칸 아래 `⊘ 먼저 카테고리를 선택해 주세요.`

카테고리를 고르기 전에는 스마트스토어가 **사진 올리기 창 자체를 열어주지 않습니다.**
그래서 어떤 셀렉터를 써도 실패할 수밖에 없었습니다.

덧붙여 이 경고 문구는 카테고리를 골라도 **DOM 에서 사라지지 않고 숨겨지기만** 합니다.
그래서 '있는지' 가 아니라 **'보이는지'** 로 판단하도록 고쳤습니다.

### 실제로 통한 방법

```
① #representImage a.btn-add-img  누르기      (누르는 순간 파일 칸이 만들어짐)
② input[type="file"][accept*="image/jpeg"]  에 파일 직접 건네기 (숨어 있어도 됨)
③ #representImage .img-list img  이 생기면 성공
```

### 화면 구조 (확인한 것)

| 확인 | 결과 |
|---|---|
| iframe | **없음** |
| Shadow DOM | **없음** |
| 처음부터 있는 `input[type=file]` | **없음** (0개) |
| 단추를 누른 뒤 | 숨은 파일 칸 **1개** 생김 (`accept="image/jpeg, image/gif, image/png, image/bmp"`, multiple 아님) |
| 기준으로 삼은 것 | **id** (`#representImage`) — 자주 바뀌는 class 해시는 쓰지 않음 |

### 실패했던 기존 방식

- `input[type="file"]` 을 **미리** 찾기 → 화면에 아예 없어서 실패
- 파일 고르기 창(filechooser) 기다리기 → 카테고리가 없어 창이 안 열려 timeout
- 카테고리 경고를 **존재 여부**로 판단 → 숨겨진 뒤에도 계속 "없음" 으로 오판

### 테스트 파일

`test-main.jpg` — 1000×1000, 38KB (스마트스토어 권장 크기)

### 기본 카테고리 (업종별) — 새로 넣음

사장님 요청으로 **한 번 정해 두면 계속 쓰는 값**을 만들었습니다.

- 파일: `%LOCALAPPDATA%\BARODU Tools\smartstore-category.json`
- 지금 값: `active: "photo"` / `사진관` / `여가/생활편의>생활편의>사진/영상 촬영`
- **업종을 나중에 바꾸실 수 있습니다** — 그 파일에 다른 업종을 더하고 `active` 만 바꾸면 됩니다.
- 저장된 값이 없으면 **자동으로 넣지 않고** "직접 골라주세요" 라고 알립니다. 추측하지 않습니다.

> ⚠ 이 자동 넣기는 **아직 실제 화면에서 확인하지 못했습니다** (오늘은 사장님이 직접 고르신 상태로 시험).

### 고친 파일 (BARODU Tools `publishers/naverSmartStore` 만)

| 파일 | 한 일 |
|---|---|
| `selectors.js` | 대표사진 업로드 3단계를 **확인된 값**으로 기록 (`verified: true`) · 카테고리 조건 · 썸네일 확인식 |
| `index.js` | 성공한 방법으로 교체 · 카테고리 조건 먼저 확인 후 **되풀이하지 않고 중단** · 성공 판정을 **썸네일 표시**로 · 기본 카테고리 넣기 |
| `category.js` | **새 파일** — 업종별 기본 카테고리 저장/변경 |

PAGE MAKER 와 기존 collector·image proxy 는 **한 줄도 고치지 않았습니다.**
(모듈 읽기 시험으로 기존 기능 회귀 없음 확인)

### 다음 단계

**추가사진 업로드**

---

## 추가사진 업로드 — **성공** (2026-09-16 낮)

### 결과

| 항목 | 결과 |
|---|---|
| 대표사진 업로드 | **성공** (그대로 유지됨) |
| **추가사진 업로드** | **성공** |
| 화면 썸네일 확인 | **확인함** — `추가이미지 (1/9)` 로 표시 |
| 올라간 주소 | `shop1.phinf.naver.net/...` (네이버 서버에 실제 전송됨) |
| 로그인 세션 | 유지됨 |
| 저장·임시저장·등록 | **누르지 않음** |

### 실제 selector

| 무엇 | 값 |
|---|---|
| 추가사진 영역 | `#optionalImages` (`<ncp-product-temp-photo-infra-image-upload id="optionalImages">`) |
| 추가 단추 | `#optionalImages a.btn-add-img` (`data-nclicks-code="image.add"`) |
| 파일 칸 | `input[type="file"][accept*="image/jpeg"]` — **단추를 누른 뒤에 생김** |
| 썸네일 확인 | `#optionalImages .img-list img` |

### 실제 사용한 업로드 방식

**대표사진에서 성공한 흐름을 그대로 재사용했습니다.** 새 우회 로직을 만들지 않았습니다.

```
① #optionalImages a.btn-add-img 누르기   ← 이때 파일 칸이 생김
② 그 숨은 파일 칸에 파일 직접 건네기       ← setInputFiles (1장)
③ #optionalImages .img-list img 생기면 성공
```

### 대표사진과 다른 점

딱 하나입니다 — 여기 만들어지는 파일 칸은 **`multiple`** 이라 여러 장을 한 번에 받습니다.
(대표사진은 `multiple` 아님, 1장 전용)

### 화면 구조 (확인한 것)

| 확인 | 결과 |
|---|---|
| iframe | **없음** |
| Shadow DOM | **없음** |
| 클릭 전 `input[type=file]` | **0개** |
| 클릭 후 | **1개** 생김 (숨김, `accept="image/jpeg, image/gif, image/png, image/bmp"`, multiple) |

### 테스트 파일

`test-extra.jpg` — 1000×1000, 38KB (대표사진과 구분되도록 색·글자를 다르게 만듦)

### 고친 파일

`publishers/naverSmartStore/selectors.js` — 추가사진을 `verified: true` 로 표시.
**업로드 로직은 고치지 않았습니다** (대표사진 코드를 그대로 씁니다).
PAGE MAKER·기존 collector·image proxy 는 손대지 않았습니다.

### 다음 작업

**상세페이지 분할 JPG 1장 업로드 테스트**

---

## 상세페이지 JPG 업로드 — **실패 (중단)** (2026-09-16 낮)

### 지금까지 결과

| 단계 | 결과 |
|---|---|
| 대표사진 1장 | **성공** (썸네일 확인) |
| 추가사진 1장 | **성공** (썸네일 확인) |
| **상세페이지 JPG 1장** | **실패 — 편집기를 열지 못함** |
| 화면 확인 | 상세페이지는 **올리지 못했으므로 확인 못 함** |
| 임시저장·저장·등록 | **누르지 않음** |

### 상세페이지 영역의 실제 DOM

상세설명은 사진 칸이 아니라 **SmartEditor ONE 을 따로 여는 구조**입니다.

```
<textarea name="editorContent" ...>      ← 비었는지만 표시하는 칸 (여기로는 못 넣음)
<a ng-click="vm.func.changeEditorType(SEONE)">   직접 작성
<a ng-click="vm.func.changeEditorType(NONE)">    HTML 작성
<button class="btn btn-primary btn-lg"
        ng-click="vm.func.openEditor($event, false)">  SmartEditor ONE 으로 작성
<button class="btn btn-gradient btn-lg"
        ng-click="vm.func.openEditor($event, true)"
        data-nclicks-code="aidescr...">              AI 상세설명 작성하기
```

| 확인 | 결과 |
|---|---|
| iframe | 상품등록 화면에는 **없음** |
| Shadow DOM | **없음** |
| `input[type=file]` | 상세설명 영역에 **없음** |
| SmartEditor 여부 | **맞음** — SmartEditor ONE 을 단추로 연다 |
| 편집기 창 | 눌러도 **새 창이 뜨지 않음** (윈도우 목록으로 확인) |

### 사용한 selector

- `button.btn-primary.btn-lg` (1차)
- `button[ng-click='vm.func.openEditor($event, false)']` (2차, 더 정확한 것)
- 모달 닫기: `button.close[aria-label="닫기"]` (`ng-click="vm.popupNoticesShow = false"`)

### 실패한 단계

`SmartEditor ONE 으로 작성` 을 눌렀지만 **편집기가 열리지 않음.**
새 창도, iframe 도 생기지 않았습니다. 그래서 파일을 건넬 대상 자체가 없었습니다.

중간에 확인한 것: 화면을 가리던 **안내 모달이 계속 열려 있었고**, 올바른 닫기 단추를
찾아 닫은 뒤 다시 눌러도 결과는 같았습니다. (2회 시도 후 지시대로 중단)

### 대표·추가사진과 다른 점

| | 대표·추가사진 | 상세페이지 |
|---|---|---|
| 방식 | 같은 화면 안에서 처리 | **다른 편집기(SmartEditor ONE)** 를 열어야 함 |
| 단추를 누르면 | 숨은 `input[type=file]` 이 **생김** | **아무것도 생기지 않음** |
| 우리가 붙을 곳 | 그 파일 칸 | 아직 못 찾음 |

### 가장 유력한 해결 방법

1. **편집기가 별도 창(팝업)으로 뜨는지 다시 확인** — 지금 도구는 처음 연 창 하나만 보고
   있어서 새 창을 못 봅니다. 도구가 `context.pages()` 전체를 보도록 고쳐야 합니다.
   (이번에 윈도우 목록으로는 새 창이 안 보였지만, 창이 만들어지자마자 닫혔을 수 있습니다)
2. 그래도 안 열리면 **카테고리 등 앞 조건** 때문일 수 있습니다. 사진이 그랬던 것처럼
   앞 항목을 채운 뒤에야 열리는지 확인이 필요합니다. (이번 화면은 카테고리가 비어 있었습니다)
3. 위 두 가지로 안 되면 `HTML 작성` 탭으로 바꿔(`changeEditorType(NONE)`)
   이미지 태그를 넣는 길이 있는지 확인.

### 테스트에 쓰려던 파일

`Downloads\새_상세페이지_01.jpg` — PAGE MAKER 가 실제로 만든 분할 JPG (82KB). 아직 쓰지 못함.

### 고친 코드

**없습니다.** 상세페이지는 원래부터 `verified: false` 로 두고 "직접 올려주세요" 라고
알리게 되어 있어 그대로입니다. 대표·추가사진 성공 코드는 **건드리지 않았습니다.**

### 이번에 생긴 문제

확인용 창이 세 번 중단되어 그때마다 **상품등록 입력 상태(대표·추가사진 포함)가 사라졌습니다.**
저장 전이라 남지 않습니다. 로그인은 마지막에는 유지됐습니다.

### 다음 작업

**상세페이지 JPG 1장 업로드 (재시도)** — 먼저 편집기가 어디에 열리는지부터 확정할 것

---

## 로그인 세션 유지 — **해결** (2026-09-16 낮)

### 원인 (코드 감사 결과)

| 확인 항목 | 결과 |
|---|---|
| persistent profile 경로 | `%LOCALAPPDATA%\BARODU Tools\smartstore-profile` — **고정, 매번 동일** |
| `launchPersistentContext` | ✔ 사용 중 (`session.js` 한 곳뿐) |
| 실행마다 임시 폴더 | **아님** (`barodu-ss-` 임시 폴더는 사진 파일용, 브라우저와 무관) |
| `browser.newContext()` | 스마트스토어에는 **없음** (링크 읽기 쪽만 — 별개 브라우저) |
| `browser.close()` | 스마트스토어 코드에 **없음** |
| `context.close()` | `closeStoreWindow()` 안에만 — 자동화 흐름에서 **호출 안 됨** |
| `page.close()` | **없음** |
| 작업마다 새 브라우저 | **아님** |
| Chromium 강제 종료 코드 | 자동화에는 **없음** (`paste.js` 의 `Stop-Process` 는 메모장만 대상) |

**진짜 원인 — 코드가 아니라 시험 방식이었습니다.**

1. 시험용 도구가 브라우저의 주인이었고, 그 프로세스가 중단될 때마다
   Playwright 가 **Chromium 을 강제 종료**했습니다.
2. 정리한다고 `Stop-Process -Force` 로 Chromium 을 직접 죽인 적이 2회 있었습니다.

Chromium 은 로그인 쿠키를 메모리에 두고 **정상 종료할 때** 파일에 씁니다.
강제로 죽이면 방금 한 로그인이 저장되지 않습니다.
`제대로 종료되지 않았습니다 / 페이지를 복원하시겠습니까?` 도 같은 이유입니다.

### 고친 것 (`publishers/naverSmartStore` + `server/routes/publish.js` 만)

- **정상 종료 고리** — SIGINT·SIGTERM·Electron 종료 때 `context.close()` 로 곱게 닫아 쿠키가 저장되게
- 복원 안내창 방지 — `--hide-crash-restore-bubble`, `--disable-session-crashed-bubble`
- **기존 창·기존 페이지 우선 재사용**, 이미 상품등록 화면이면 메뉴를 다시 걷지 않음
- 로그인 / **추가 인증** / 모름을 구분해 안내 (추가 인증은 자동으로 풀지 않음)
- 로그인 판정을 주소가 아니라 **판매자 메뉴가 실제로 보이는지**로
  (`/home` 소개 화면도 `로그인 필요` 로 정확히 잡음)
- 생성 횟수 계측 `counts` + 상태 확인 경로 `GET /v1/publish/smartstore/status`

### 실제 테스트 결과 (실측값)

| 시험 | 결과 |
|---|---|
| browser 생성 | **1회** (`counts.browser = 1`) |
| 새 page 생성 | **0회** (`counts.page = 0`, 재사용만) |
| 상품등록 화면 이동 후 로그인 | **유지** |
| 자동화 연속 실행 (3회) | **전부 성공**, 매번 로그인 유지 |
| **서버를 껐다 켠 뒤** | **로그인 그대로 살아있음** ← 이번 수정의 핵심 증거 |
| 브라우저 자동 종료 | **없음** (작업이 끝나도 창이 열려 있음) |
| 상품명·판매가 입력 | `붙여넣기` 방식으로 성공 |
| 저장·임시저장·등록 | **누르지 않음** |

### 추가 인증 처리 방식

자동으로 풀지 않습니다. 화면에 이렇게만 알립니다:

> 네이버 추가 인증이 필요합니다. 브라우저에서 직접 완료한 후 다시 눌러주세요.
> BARODU 는 인증을 대신 풀지 않습니다.

인증 후에는 **같은 창·같은 세션**에서 이어집니다 (새 브라우저를 만들지 않습니다).

### 브라우저 종료 조건

- 사장님이 직접 창을 닫을 때
- BARODU Tools 를 끌 때 (이때 **곱게** 닫습니다)
- 그 외에는 닫지 않습니다. 작업이 끝났다고 닫지 않습니다.

### 기존 성공 기능

- 대표사진 1장 업로드 — 코드 **그대로** (`verified: true`)
- 추가사진 1장 업로드 — 코드 **그대로** (`verified: true`)
- 이번 수정으로 되돌린 것 없음

### 남은 문제

- 이번 세션 유지 시험 중에는 사진을 올리지 않았습니다.
  (창이 새로 열려 예전에 올린 사진은 남아 있지 않습니다 — 저장 전이라 정상입니다)
- 상세페이지(SmartEditor ONE)는 여전히 미해결 상태 그대로입니다.

### 다음 시작점

**상세페이지 분할 JPG 1장 업로드**

---

## 여기서 안전 중단 (2026-09-16 오후 — 네이버 보호조치)

네이버 아이디 보호조치로 비밀번호 변경이 필요해져 **작업을 안전하게 멈췄습니다.**
강제 종료 없이 곱게 닫아 로그인 상태와 프로필을 보존했습니다.

### 지금까지 성공한 것 (다시 개발하지 말 것)

| 기능 | 상태 | 확인 방법 |
|---|---|---|
| 로그인 세션 유지 | **완료** | 서버 재시작 후에도 로그인 유지 확인 |
| 대표사진 1장 업로드 | **성공** | 화면 썸네일 + 네이버 서버 주소 확인 |
| 추가사진 1장 업로드 | **성공** | 화면 `추가이미지 (1/9)` 썸네일 확인 |
| 상품명 입력 | **성공** | 붙여넣기 방식, 화면에 값 들어감 |
| 판매가 입력 | **성공** | 붙여넣기 방식, 화면에 값 들어감 |
| **상세페이지 JPG 1장** | **성공 보고됨 (눈으로는 미확인)** | 아래 설명 |

### 상세페이지 JPG — 마지막 시험 결과

PAGE MAKER 가 실제로 만든 분할 JPG(`새_상세페이지_01.jpg`) 1장으로 돌린 결과:

```
스마트스토어 열기      done
로그인 확인            done
상품등록 화면 열기      done
상품명 입력            done  (붙여넣기)
상세페이지 올리기       done  (1장, 파일 칸)
최종 등록 전에서 멈춤   done  (저장하기·임시저장 누르지 않음)
```

성공 판정은 **편집기 안에 사진이 실제로 나타난 것을 세어서** 했고,
그 뒤 편집기의 `등록` 으로 상품 양식에 옮겨 담아 편집기 탭이 닫힌 것까지 확인했습니다.
다만 **상품 양식의 상세설명 칸을 화면으로 직접 눈으로 보지는 못했습니다.**
(보호조치 안내를 받고 바로 중단했습니다.) → 다음에 그것부터 확인하면 됩니다.

### SmartEditor ONE 확인 상태

| 확인 | 결과 |
|---|---|
| 어디에 열리는가 | **같은 브라우저의 새 탭** (`#/editor`) — 팝업 창이 아님 |
| iframe | 편집기 안에 `input_buffer...` 하나 (본 화면에는 없음) |
| Shadow DOM | 없음 |
| 사진 추가 단추 | `button.se-image-toolbar-button` |
| 파일 칸 | 단추를 누른 뒤 생김 (대표사진과 같은 방식) |
| 편집기 `등록` | 상세설명을 **상품 양식으로 옮겨 담는** 단추. 상품 등록이 아님 |

### persistent profile

- 실제 경로: `C:\Users\notebook\AppData\Local\BARODU Tools\smartstore-profile`
- **보존됨** (삭제·초기화·경로 변경 하지 않음)
- 쿠키 파일 32KB, 최종 기록 2026-09-16 12:43
- ⚠ 비밀번호를 바꾸시면 네이버가 기존 세션을 무효화할 수 있습니다.
  그때도 **같은 프로필을 계속 씁니다.** 새로 로그인만 하시면 됩니다.

### 이번에 수정한 파일 (BARODU Tools 만)

| 파일 | 내용 |
|---|---|
| `publishers/naverSmartStore/session.js` | 창·페이지 재사용, 로그인/추가인증 판정, 정상 종료, 생성 횟수 계측 |
| `publishers/naverSmartStore/detail.js` | **새 파일** — SmartEditor ONE 열기·사진 올리기·상품 양식으로 옮기기 |
| `publishers/naverSmartStore/index.js` | 새 로그인 판정 사용, 상세페이지 연결, 기존 화면 재사용 |
| `publishers/naverSmartStore/selectors.js` | 실제 확인값 (대표/추가사진 `verified: true`) |
| `publishers/naverSmartStore/category.js` | 업종별 기본 카테고리 (사진관) |
| `server/routes/publish.js` | 상태 확인 / 편집기 확인 / **곱게 닫기** 경로 |

PAGE MAKER · 기존 collector · image proxy 는 **손대지 않았습니다.**

### 아직 테스트하지 못한 것

- 상세설명 칸에 실제로 이미지가 들어갔는지 **눈으로 확인**
- 분할 JPG **여러 장 순서대로** 업로드
- 상품 설명(글) 입력 — 스마트에디터 경유
- 이벤트·혜택 (스마트스토어에 대응 칸이 없어 `직접 확인 필요` 로 둠)
- 통합 자동입력 한 번에 (상품명 + 가격 + 사진 + 상세)
- 등록자료 ZIP / TXT 재확인
- BARODU Tools 새 버전 · Setup.exe (아직 1.0.5 그대로)
- PAGE MAKER 최종 build · 배포

### 남은 문제

- 네이버 아이디 보호조치 → 비밀번호 변경 필요 (사용자가 직접 처리)
- 설치본 BARODU Tools 는 **1.0.5** 그대로 — 스마트스토어 기능은 아직 들어 있지 않음

### 다음 시작 위치

**네이버 보호조치 해제 및 새 비밀번호 로그인 완료 후
→ persistent profile 로그인 상태 확인
→ 상품 양식의 상세설명 칸에 이미지가 들어갔는지 눈으로 확인
→ 분할 JPG 여러 장 순서대로 업로드부터 재개**

(로그인 유지·대표사진·추가사진·상품명·판매가는 이미 성공 — 다시 만들지 않습니다)

---

## 완주 테스트 1차 — **배포 보류** (2026-09-16 오후)

실제 사용자 흐름으로 끝까지 돌려보는 중입니다.
**로그인이 필요 없는 항목은 전부 통과**했고, 스마트스토어 자동입력 부분은
네이버 로그인이 아직 되지 않아 **확인하지 못했습니다.**

### 통과한 것 (실제 화면·실제 파일로 확인)

| 항목 | 결과 |
|---|---|
| PAGE MAKER 완성·저장 → 네이버 스마트스토어 영역 | 통과 (준비 상태 9줄 정상) |
| 항목별 복사 (7개) | 통과 |
| 전체 문구 복사 | 통과 |
| 등록자료 ZIP 생성 | 통과 — 725KB |
| ZIP 폴더 구조 | 통과 — `01_대표사진`/`02_추가사진`/`03_상세페이지`/`04_입력문구` |
| 사진 파일 이름 | 통과 — `광명가족사진_감동사진관_대표.jpg`, `광명가족사진_가족촬영_01.jpg` … (검색어 반복 없음) |
| 상세페이지 분할 3장 순서 | 통과 — `_상세_01/02/03` |
| TXT 내용 | 통과 — 사람이 그대로 복사 가능, 개발용 데이터 없음 |
| 데이터 동일성 | 통과 — 화면 복사·TXT·ZIP 이 모두 `services/smartstore/payload.ts` 한 곳에서 나옴 |
| 전체 JPG | 통과 (582KB) |
| 자동저장 | 통과 |
| 실행취소 | 통과 |
| 작업파일(.saypage) 저장 | 통과 (197KB) |
| 도구 7개 열림 (상품정보·사진·구성·디자인·PROMPTER·내 작업·완성) | 통과 |
| 폰트 툴바 | 통과 (제목/소제목/본문/직접/B/정렬 3종) |
| 사진 원본 보호 안내 | 통과 |
| Tools `/health`, `/api/health` | 통과 (1.0.5) |
| Tools 링크 가져오기 | 통과 (실제 수집 성공) |
| Tools image proxy | 통과 (HTTP 200, image/jpeg 872KB) |
| Tools SSRF 방어 | 통과 (내부 주소 400 차단) |

### 이번에 고친 것 (최소 수정)

`src/components/export/SmartStore.tsx` — **복사 대비책**.
브라우저가 새 복사 방식을 막을 때 예전 방식으로 한 번 더 시도한다.
고치기 전에는 `복사하지 못했습니다` 가 떴고, 고친 뒤 정상 동작을 확인했다.
(그 밖의 PAGE MAKER UI·collector·대표/추가사진 코드는 손대지 않았다)

### 확인하지 못한 것 — **네이버 로그인 필요**

| 항목 | 상태 |
|---|---|
| 로그인 세션 재사용 | 확인 못 함 (비밀번호 변경으로 세션 무효화됨) |
| PAGE MAKER 자동입력 버튼에서 시작 | 확인 못 함 |
| 상품명·판매가·상품설명·이벤트 실제 입력 | 확인 못 함 |
| 대표사진·추가사진 통합 흐름 업로드 | 확인 못 함 |
| 상세페이지 1장 / 여러 장 | 확인 못 함 |
| 최종 등록 직전 정지 | 확인 못 함 |
| 자동입력 실패 fallback | 확인 못 함 |

> 참고: 대표사진·추가사진·상세페이지 1장은 **앞선 시험에서 이미 성공**했습니다.
> 이번에 못 한 것은 **통합 흐름에서 한 번에** 도는지입니다.

### 또 하나 — 통합 테스트를 하려면

PAGE MAKER 는 5199 번의 BARODU Tools 를 찾습니다.
지금 5199 에는 **설치본 1.0.5** 가 있고 여기에는 스마트스토어 기능이 없습니다.
그래서 통합 테스트를 하려면 설치본을 잠시 끄고 **새 코드를 5199 로** 올려야 합니다.
(설치본은 시작 메뉴에서 다시 켜면 됩니다)

### 배포 가능 여부

**배포 보류** — 자동입력 통합 흐름을 아직 한 번도 완주하지 못했습니다.

### 다음 시작 위치

**네이버 간편 로그인 완료 → 설치본 Tools 잠시 끄고 새 코드를 5199 로 →
PAGE MAKER `스마트스토어 자동입력` 버튼부터 통합 흐름 완주**

---

## 스마트스토어 자동입력: **보류** / 스마트플레이스 등록자료: **완료** (2026-09-16 저녁)

### 스마트스토어 자동입력 — 일시 보류

> **스마트스토어 자동입력: 보류 — 추후 테스트용 계정/환경에서 현재 상태부터 재개**
>
> 실제 운영 중인 네이버 메인 계정의 반복 로그인·보안 인증 테스트를 줄이기 위해
> 실계정 테스트를 중단합니다. **관련 코드는 하나도 지우지 않았습니다.**

보존된 것 (모두 그대로 있음):

| 파일 | 내용 |
|---|---|
| `publishers/naverSmartStore/index.js` | 전체 흐름 (로그인 확인 → 메뉴 이동 → 글 → 사진 → 등록 직전 정지) |
| `publishers/naverSmartStore/selectors.js` | 실제 확인한 화면 위치 |
| `publishers/naverSmartStore/session.js` | **일반 Chrome + BARODU 전용 프로필**, 창 재사용, 정상 종료 |
| `publishers/naverSmartStore/detail.js` | SmartEditor ONE 열기·사진·상품 양식으로 옮기기 |
| `publishers/naverSmartStore/paste.js` | 글 넣기 (한 번에 넣기 → 클립보드 → 최후 직접입력) |
| `publishers/naverSmartStore/category.js` | 업종별 기본 카테고리 (사진관) |
| `server/routes/publish.js` | 상태·편집기확인·화면캡처·곱게닫기·자동입력 경로 |
| `src/services/smartstore/*` (PAGE MAKER) | SmartStorePayload · TXT · ZIP · 자동입력 호출 |

진행 상태:

| 항목 | 결과 |
|---|---|
| 로그인 세션 유지 구조 | **완료** (일반 Chrome + `%LOCALAPPDATA%\BARODU Tools\chrome-profile`) |
| 대표사진 1장 업로드 | **성공** (썸네일·네이버 서버 주소 확인) |
| 추가사진 1장 업로드 | **성공** (썸네일 확인) |
| 상세페이지 JPG 1장 | 코드상 성공 보고됨 — **화면 눈확인 못 함** |
| 상품명·판매가 | 들어가지만 **클립보드 경유에서 한글 깨짐 발견 → 고침** (한 번에 넣기 우선). 고친 뒤 재확인 못 함 |
| 통합 완주 | **못 함** |

**재개 지점**: 테스트용 네이버 계정 준비 → BARODU 전용 Chrome 프로필로 로그인
→ 상품명 한글 정상 입력 확인 → 상세페이지 1장 눈확인 → 여러 장 → 통합 완주

### 스마트플레이스 등록자료 — 이번에 새로 만듦

**네이버에 접속하지 않습니다.** 글을 순서대로 정리하고 복사·파일로 내주는 것만 합니다.

새 파일:

| 파일 | 내용 |
|---|---|
| `src/services/smartplace/payload.ts` | **SmartPlacePayload** — 15개 항목을 한 곳에서 만든다 |
| `src/services/smartplace/text.ts` | 전체 복사·메모장 파일 글 (항목명 중복 없음) |
| `src/components/export/SmartPlace.tsx` | 화면 (완성·저장 안) |

고친 파일: `src/components/export/StepCheck.tsx` (연결), `src/types/project.ts` (`place` 칸 추가), `src/styles/app.css`

### 실제 테스트 결과 (화면·파일로 확인)

| 항목 | 결과 |
|---|---|
| 15개 항목 순서 | 통과 (업체명 … 사진 설명) |
| 값이 항목명 바로 아래 | 통과 |
| 항목명 중복 (`업체명: …`) | **없음** |
| 개별 복사 | 통과 — **값만** 들어감 (`감동사진관`, `02-2681-0000`, `경기도 광명시 오리로 000 2층`) |
| 전체 내용 복사 | 통과 — 번호+항목명+값 (489자) |
| TXT 생성 | 통과 — `스마트플레이스_입력내용.txt` (997 bytes) |
| TXT 항목 수 | 15개, 중복 표시 없음 |
| TXT 한글·줄바꿈 | 통과 (BOM `EF BB BF` 확인 — 메모장에서 안 깨짐) |
| 값 없는 항목 | 중요항목 `확인 필요` / 나머지 `입력할 내용 없음` |
| 스마트플레이스용 문구 수정 | 통과 |
| **원본 상세페이지 보호** | 통과 — 스마트플레이스용만 바뀌고 원본 한 줄 소개는 그대로 |
| 네이버 사이트 접근 | **접근하지 않음** (코드에 네트워크 호출 0건, 통신 기록 0건) |

### 곁다리로 고친 것

`src/components/export/SmartStore.tsx` — 복사 대비책.
브라우저가 새 복사 방식을 막을 때 예전 방식으로 한 번 더 시도한다. (복사 실패 문제 해결)

### 환경 원복

설치본 **BARODU Tools 1.0.5** 를 다시 켜 두었습니다 (5199 정상).
개발판 서버는 껐습니다. 코드는 그대로 남아 있습니다.

### 다음 시작점

- **스마트플레이스**: 완료. 실제 사용해 보시고 불편한 점 알려주시면 다듬기
- **스마트스토어**: 테스트용 계정 준비되면 위 재개 지점부터

---

## 배포 완료 — 스마트플레이스 등록자료 (2026-09-16 저녁)

### 배포 방식 (기존 방식 그대로)

```
npm run build  →  dist
scripts/serve.mjs  →  http://localhost:5180 (dist 를 그대로 내보냄)
cloudflared quick tunnel  →  공개 주소
```

새 배포 구조를 만들지 않았습니다. WORKLOG 에 적힌 기존 방식과 같습니다.

**공개 주소**: `https://interstate-forget-joins-ind.trycloudflare.com`

> 노트북을 끄면 주소가 사라지고, 다시 열면 주소가 바뀝니다.
> 내용을 바꾼 뒤에는 `npm run build` 만 하면 공개 주소에 바로 반영됩니다.

### 공개 주소에서 직접 확인한 것

| 확인 | 결과 |
|---|---|
| 스마트플레이스 등록자료 영역 표시 | 통과 |
| `01. 업체명` 아래에 값 바로 표시 | 통과 |
| 항목명·값 중복 표시 | **없음** |
| 개별 복사 = 값만 | 통과 (`감동사진관`, `02-2681-0000`, `경기도 광명시 오리로 000 2층`, `화~토 10:00~19:00`) |
| 전체 내용 복사 | 통과 (번호+항목명+값) |
| `스마트플레이스_입력내용.txt` 생성 | 통과 (778 bytes, 15항목) |
| TXT 한글·줄바꿈 | 통과 (BOM `EF BB BF`) |
| 수정 후 복사·TXT 즉시 반영 | 통과 |
| PC 화면 | 통과 (가로 스크롤 없음) |
| 모바일 375px | 통과 (고친 뒤) |
| 기존 기능 회귀 | 통과 (모드탭·도구 7개·링크 가져오기·스마트스토어 영역 전부 정상) |

### 이번에 고친 것 (최소 수정 1건)

`src/styles/app.css` — `.sp__value` 에 `overflow-wrap:anywhere` 추가.
모바일에서 주소·전화번호가 가로로 넘치던 것을 고쳤습니다. 고친 뒤 공개 주소에서 다시 확인했습니다.

### 스마트스토어 자동입력

**보류 상태 그대로.** 코드는 하나도 지우지 않았고 이번에 손대지도 않았습니다.
실계정 테스트도 하지 않았습니다.

### 남은 문제

- 공개 주소는 **이 노트북이 켜져 있는 동안만** 열립니다 (cloudflared 임시 터널)
- 공개 주소에서 `링크로 가져오기` 는 보는 분 컴퓨터에 BARODU Tools 가 설치되어 있어야 동작합니다
- 스마트스토어 자동입력은 보류 (테스트용 계정 준비 후 재개)

---

# ★ BARODU PAGE MAKER 1차 완성본 (2026-09-16)

**다음 단계: 1차 서포터즈 테스트.** 이 뒤로는 새 기능을 계속 붙이지 않습니다.

**공개 주소**: `https://interstate-forget-joins-ind.trycloudflare.com`
(노트북이 켜져 있는 동안만 열립니다. 내용을 바꾸면 `npm run build` 만 하면 반영됩니다.)

## 이번에 마무리한 것

| 항목 | 내용 |
|---|---|
| 첫 사용자 안내 | 자동입력 맨 위에 `처음이시라면 자동입력부터 시작해보세요.` 한 줄 |
| 주소 자동 판별 | 블로그 · 스마트플레이스 · **스마트스토어** · **인스타그램** · 홈페이지 **5종** |
| 정직한 안내 | 아직 잘 못 읽는 곳(스마트스토어·인스타)은 화면에서 **그대로 알림** |
| PROMPTER 설명 | `AI에게 수정 요청할 문장을 만들어드립니다.` |
| BARODU Tools 안내 | 왜 필요한지 + 다른 BARODU 기능에서도 함께 쓴다는 설명 추가 |
| 완성·저장 | ① 저장하기 ② 스마트스토어 ③ 스마트플레이스 **세 구역으로 구분** |
| 모바일 | 글자 도구막대가 넘치던 것 고침 |

> `간편모드 / 전체모드` 는 만들지 않았습니다. `자동입력 / 직접입력` 그대로입니다.

## 이미 되어 있어 그대로 둔 것 (실제 화면에서 다시 확인함)

글자 툴바(제목·소제목·본문·직접·B·정렬 3종) · 프리셋 활성표시 · 폰트 5종 ·
전체 글꼴/이 섹션만 · 디자인 미니 카드 6개 · 미리보기 직접 수정 ·
사진 업로드·대표·순서변경·삭제·사용위치 · 자동저장 · 실행취소 ·
분할/전체 JPG · ZIP · .saypage · 스마트스토어 등록자료 · 스마트플레이스 등록자료

## 기존 정보 가져오기 — 사이트별 현재 상태

| 사이트 | 자동 판별 | 실제 수집 |
|---|---|---|
| 네이버 블로그 | 됨 | **됨** (실제 주소로 글 + **사진 6장** 확인) |
| 네이버 스마트플레이스 | 됨 | 됨 (화면이 자주 바뀌어 못 읽을 때 있음) |
| 홈페이지 | 됨 | 됨 |
| 네이버 스마트스토어 | 됨 | **전용 수집기 없음** — 일반 방식으로만 시도, 일부만 들어올 수 있음 |
| 인스타그램 | 됨 | **대부분 못 읽음** — 로그인해야 보이는 글이 많음. 화면에서 그대로 안내 |

기존값 보호: **됨** — 가져온 값은 확인 화면에서 켜야 적용되고,
이미 적어두신 값은 `지금 적어두신 값: …` 로 보여주며 끄면 그대로 둡니다.

## 이번에 하지 못한 것 (정직히)

- **스마트스토어·인스타그램 전용 수집기** — 새로 만들지 못했습니다.
  (Tools 새 버전 + 설치가 필요하고, 인스타는 로그인 없이 읽기 어렵습니다)
- **여러 주소 한 번에 넣기 / 자료풀 합치기 / 중복 사진 그룹화 / 출처별 구분**
  — 1차 범위에서 제외했습니다. 지금은 주소를 하나씩 넣어 여러 번 가져오면 됩니다.
- **스마트스토어 자동입력** — 보류 상태 그대로 (아래)

## 스마트스토어 자동입력 — 보류 유지

> 실제 운영 중인 네이버 메인 계정의 반복 로그인·보안 인증 테스트를 줄이기 위해 보류.
> 관련 코드는 **하나도 지우지 않았고 이번에 손대지도 않았습니다.**
> 추후 테스트용 계정/환경에서 현재 상태부터 재개합니다.

보존: publisher · persistent profile(일반 Chrome + BARODU 전용) · 로그인 세션 코드 ·
대표사진 업로드(성공) · 추가사진 업로드(성공) · 상세페이지 자동화 코드 · SmartStorePayload

## 이번에 고친 파일

- `src/services/import/baroduTools.ts` — 주소 5종 판별 + 사이트별 현재 수준 안내
- `src/components/ai/ImportUrl.tsx` — 주소칸 문구 · 판별 안내 · Tools 설명
- `src/components/editor/EditorPanel.tsx` — 첫 사용자 안내 · PROMPTER 설명
- `src/components/export/StepCheck.tsx` — 완성·저장 세 구역
- `src/styles/app.css` — 구역 제목 · 모바일 도구막대 넘침

## 알려진 제한 (서포터즈에게 안내할 것)

1. 공개 주소는 노트북이 켜져 있는 동안만 열립니다
2. `링크로 가져오기` 는 그 컴퓨터에 **BARODU Tools** 가 설치·실행되어 있어야 합니다
3. 스마트스토어·인스타 주소는 아직 일부만 들어오거나 못 읽을 수 있습니다
4. 스마트스토어 **자동입력**은 보류 (등록자료 받기·복사는 정상)
5. 상세페이지 이미지를 만들 때 사진이 많으면 시간이 걸립니다

## 다음 단계

**1차 서포터즈 테스트** — 실제 사진관 사장님이 끝까지 만들어 보시고
막히는 곳·이해 안 되는 곳을 모아 2차 개선에 반영합니다.

---

## 1차 공개 준비 — 저장소 준비 완료 / 배포는 로그인 대기 (2026-09-16 밤)

### 마친 것

| 항목 | 결과 |
|---|---|
| 읽는 규칙 자동 시험 | **70/70 통과** (자료 13곳) |
| build | **성공** (오류 0) |
| git 저장소 | **만듦** — `SAY-PAGE-MAKER-PRO` 폴더, 브랜치 `main` |
| 첫 커밋 | 106개 파일 (`node_modules`·`dist` 제외) |
| 1차 버전 보존 | 태그 **`v1-supporters`** |
| 민감정보 검사 | **없음** — 로그인·쿠키·프로필·토큰 한 건도 포함되지 않음 |
| `.gitignore` | 새로 만듦 (프로필·쿠키·토큰·node_modules·dist 차단) |

### ⚠ 공개 주소에서 `링크로 가져오기`가 안 되는 문제 (확인함)

공개 HTTPS 주소에서 BARODU Tools 를 부르면 **막힙니다.**

원인: `BARODU Tools/server/cors.js` 가 **이 컴퓨터 안 주소(localhost·127.0.0.1)에서 온 요청만** 받도록
되어 있습니다. 바깥 주소는 403 으로 막습니다. **일부러 그렇게 만든 안전장치**입니다.

실측:
- 공개 주소에서 `http://127.0.0.1:5199/health` 호출 → 막힘
- 화면 표시 → `BARODU Tools 필요`

즉 **공개 주소에서는 블로그·플레이스·홈페이지 가져오기를 쓸 수 없습니다.**
(그 밖의 기능 — 직접입력·사진·구성·디자인·미리보기 수정·저장·스마트플레이스 등록자료 — 는 모두 정상)

고치려면 Tools 가 그 공개 주소를 허용해야 하는데, 그러면 **아무 웹사이트나 이 컴퓨터의 Tools 를
부를 수 있게 되는 위험**이 생깁니다. 사장님 결정이 필요한 부분이라 임의로 바꾸지 않았습니다.

### 아직 못 한 것 — 로그인이 필요합니다

| 필요한 것 | 상태 |
|---|---|
| GitHub CLI(`gh`) | **설치 안 됨** |
| GitHub 로그인 | 없음 (자격증명 관리자에도 없음) |
| Cloudflare 로그인 / wrangler | 없음 |

BARODU 가 사장님 계정에 **대신 로그인하지 않습니다.** 로그인은 직접 하셔야 합니다.

### 다음 시작점

1. GitHub 로그인 → 저장소 만들고 올리기 (태그 `v1-supporters` 포함)
2. Cloudflare Pages 연결 (build `npm run build`, 나오는 곳 `dist`, 공개 브랜치 `main`)
3. 고정 주소 `*.pages.dev` 확인
4. 공개 주소에서 최종 점검
5. `링크로 가져오기` 를 공개 주소에서도 쓸지 결정 (위 안전장치 문제)
# 쓰는 법 (명령어 필요 없음)

## 1. BARODU Tools 는 이미 켜져 있습니다

컴퓨터를 켜면 자동으로 함께 켜집니다. 따로 하실 일이 없습니다.
(화면 오른쪽 아래 시계 옆 작은 아이콘)

## 2. 이 파일을 더블클릭하세요

```
C:\Users\notebook\Downloads\클로드\SAY-PAGE-MAKER-PRO\BARODU PAGE MAKER 시작.cmd
```

검은 창이 잠깐 뜨고, 브라우저가 알아서 열립니다.

주소: **http://localhost:5180**

> 다 쓰신 뒤에는 그 **검은 창을 닫으면** 됩니다.
> 창을 닫아도 지금까지 한 작업은 저장돼 있습니다.

## 3. 링크 가져오기는 여기서

화면이 열리면 → **AI로 빠르게 만들기** → **STEP 3 기존 정보 가져오기**
→ **링크로 가져오기** 칸에 주소를 붙여넣고 **불러오기**

어떤 사진관 주소든 됩니다. 정해진 주소만 되는 것이 아닙니다.

- 네이버 블로그 (예: `https://blog.naver.com/...`)
- 네이버 스마트플레이스 (예: `https://m.place.naver.com/place/.../home`)
- 일반 홈페이지 (예: `http://www.○○○.com`)

입력칸 위에 **BARODU Tools 연결됨 · 1.0.5** 라고 작게 보이면 준비된 것입니다.

---

# 직접 해보실 흐름

1. 위 파일 더블클릭 → 브라우저 열림
2. **AI로 빠르게 만들기** 고르기
3. **STEP 3 기존 정보 가져오기**
4. 사진관 주소 붙여넣기
5. **불러오기**
6. **가져온 정보 확인** — 여기서는 아직 아무것도 바뀌지 않습니다
7. 쓸 **사진 고르기**
8. **선택한 내용 적용**
9. 왼쪽 **미리보기** 확인
10. 위쪽 **상품정보**(직접입력)로 이동
11. 내용 고치기
12. 다시 **AI 제작**으로 이동 → 고친 내용 그대로 있는지
13. 새로고침(F5) → 그대로 남아 있는지
14. **완성·저장** → 이미지 저장

> **이미지 저장 주의**: 저장하는 동안 **그 창이 화면에 보여야** 끝납니다.
> 다른 창으로 가리면 "시간이 너무 오래 걸립니다" 로 멈춥니다. 프로그램 문제가 아닙니다.

---

# 현재 정상 (실제로 확인함)

| 무엇 | 확인한 내용 |
|---|---|
| BARODU Tools 1.0.5 설치본 | 5199 에서 실행 중, 컴퓨터 켜면 자동 실행 |
| 개발 서버 없이 동작 | 개발 서버를 모두 끈 상태로 확인 |
| 링크 가져오기 — 네이버 블로그 | 상호·전화·촬영상품·가격·사진 6장 |
| 링크 가져오기 — 스마트플레이스 | 상호·지역·주소·전화·영업시간·휴무·가격·사진 |
| 링크 가져오기 — 일반 홈페이지 | 상호·주소·전화·휴무·사진 |
| 배경그림만 쓰는 홈페이지 | 사진 수집됨 |
| 사진 미리보기·고르기 | 썸네일 표시, 고른 것만 들어감 |
| 사진 실제 적용 | 원본 크기 그대로 저장 |
| 가져오기 전 확인 단계 | 적용 누르기 전에는 아무것도 안 바뀜 |
| 이미 적어둔 값 보호 | 다른 값은 자동으로 꺼지고 지금 값 보여줌 |
| 자동입력 ↔ 직접입력 | 같은 내용 유지 |
| 자동저장 · 새로고침 복원 | 사진까지 복원 |
| 실행취소 / 다시하기 | 정상 |
| 전체 JPG · 분할 JPG · ZIP | 실제 파일 생성 확인 |
| 작업파일 저장 / 불러오기 | 사진 포함 복원 |
| 읽는 규칙 자동 시험 | 실제 사진관 13곳 · 70항목 전부 통과 |

---

# 보류 (실제 사용 후 다시 판단)

지우지 않고 남겨둡니다. 써보신 뒤 정말 불편한 것부터 고치겠습니다.

1. **`전화번호 보기` 를 눌러야 나오는 번호**
   일부 스마트플레이스는 번호가 단추 뒤에 숨어 있습니다.
   눌러서 여는 기능을 넣었지만 설치본에서는 잘 열리지 않습니다.
   다만 그런 가게도 아래쪽에 `휴대전화번호 …` 로 적혀 있어 **대부분 결국 읽힙니다.**

2. **촬영 구성 · 제공 항목 인식이 얕음**
   액자·보정본·원본·의상·헤어메이크업 같은 항목은 표로 적은 사진관이 적어
   아직 규칙이 단순합니다.

3. **특이한 가격표 표현**
   정상가/이벤트가로 가를 근거가 없으면 **그 밖에 찾은 가격** 에 원문 그대로 둡니다.
   넘겨짚어 틀린 값을 넣지 않기 위해서입니다.

4. **특정 페이지에서만 생기는 세부 문제**
   사진관마다 적는 방식이 달라, 실제로 써보며 자주 겪는 것부터 잡는 편이 낫습니다.

---

# 실제 사용 중 봐주실 것

써보시면서 아래를 봐주시면 다음 수정 순서를 정하기 좋습니다.

- **정보 가져오기 속도** — 기다릴 만한가 (보통 10~30초)
- **사진 고르기** — 원하는 사진을 찾기 쉬운가, 개수는 적당한가
- **잘못 읽는 정보** — 어떤 항목이 자주 틀리는가
- **빠진 정보** — 페이지에는 있는데 안 들어오는 것
- **직접 수정** — 고치는 게 편한가, 어디를 고쳐야 할지 바로 보이는가
- **자동저장** — 불안한 순간이 있는가
- **저장 과정** — 이미지/ZIP 저장이 이해되는가

---

# 다음 수정 후보 (피드백 받고 결정)

- 위 **보류** 4가지 중 실제로 걸리는 것
- 사진 고르기 편의 (크게 보기, 한 번에 고르기 등)
- 촬영 구성·제공 항목 읽기 규칙 넓히기
- 코드 서명 인증서 (배포 직전)
- 자동 업데이트 주소 연결

---

# 문제가 생기면

**화면 위에 "BARODU Tools 필요" 또는 "꺼짐" 이라고 나올 때**
시작 메뉴에서 **BARODU Tools** 를 실행한 뒤, 그 안내문 옆 **다시 확인** 을 누르세요.

**검은 창에 빨간 글씨가 뜰 때**
그 창을 닫고 다시 더블클릭해 보세요.

**작업이 사라진 것 같을 때**
자동저장돼 있습니다. 브라우저를 다시 열면 이어서 나옵니다.
더 확실히 남기려면 **내 작업 → 작업파일 저장** 을 쓰세요.

---

## 기록 위치

| 무엇 | 어디 |
|---|---|
| 이 문서 | `SAY-PAGE-MAKER-PRO\WORK_STATUS.md` |
| BARODU Tools 상태 | `BARODU Tools\WORK_STATUS.md` |
| 새 버전 내는 법 | `BARODU Tools\docs\RELEASE.md` |
| 지금까지의 작업 기록 | `SAY-PAGE-MAKER-PRO\WORKLOG.md` |
| 읽는 규칙 자동 시험 | `SAY-PAGE-MAKER-PRO\tests\` (`npm run test:parse`) |
